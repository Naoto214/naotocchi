# 状態通知の調整・UIイラスト追加の検証

基準main: `f847cd78dcf652601bf4c960257e673d81c459d2`。
作業ブランチ: `feature/ui-illustrations-followup-20260910`。
この文書を含む確定HEAD・tree・CI・統合状態は当該PR本文と最新GitHubを正とする。
この追加ブランチは保存時点で未マージ・未公開。全体開発は未完了。

## 先行PRの統合・公開確認

| 対象 | 確認結果（UTC） |
| --- | --- |
| #224 | 2026-09-10 12:17:55にマージ。merge `dd224cba7e5ae86b972d1766c1d0ecdc7b758e22` |
| #225 | 12:18:12にマージ。merge/main `f847cd78dcf652601bf4c960257e673d81c459d2` |
| main tree | `c606e90928e2171d5fda39a9c099935e81bb8b93`。#225検証treeと一致 |
| Runtime #296 | [SUCCESS](https://github.com/Naoto214/naotocchi/actions/runs/34475969178) |
| Pages #223 | [SUCCESS](https://github.com/Naoto214/naotocchi/actions/runs/34475968075) |
| 公開ファイル | 12:35:53、HTML・script.js・care-status.js/CSS・care atlasの5件がHTTP 200、全てmainとバイト一致 |

[公開ゲーム](https://naoto214.github.io/naotocchi/)の配信ファイルを直接取得した結果である。
初回イラスト・状態通知は公開済み。実画面を取得できたという意味ではない。
取得URL・SHA-256は[機械記録](ui-illustrations-followup-2026-09-10.json)に残す。
再開時および実装後の再確認では他のopen PRは旧#92のみ。#92は変更しない。

## 調整と自動確認

- 長い保存病名が先頭を占め、固定2行内の操作案内が後方へ追いやられる構成を確認。
  健康0・低健康・通常の病気で「くすり」を病名より先に表示する。
  元の病名は通知データに残す。死亡条件・通知優先度・薬の効果は変えない。
- 空腹／睡眠中の空腹に既存の空の皿を接続。
- メニュー6種、装備15商品、4ごほうび、7おたのしみ、時刻・天候へ絵を接続。
  36新規素材と3用途の既存素材を再利用。保存ID・効果・価格・説明はそのまま。
- 独立レビューで、画像失敗時におたのしみボタンが空になる問題を確認。
  同じ固定枠で元の絵文字を表示する回帰を追加し、失敗→修正後成功を確認。
  CSS atlasの非表示imgで失敗を検知し、状態・所持数・装備は変更しない。
- QA計測の画像読込状態をコピーして保存し、後のloadイベントで過去の結果が書き換わらないようにした。
- 代替表示でCSS背景を消した後も、非表示imgのURLをQA画像一覧へ含める。
  画像失敗を見落とす経路を生成済みQAコードで再現し、修正後の検出と過去計測の不変を確認。

`npm test` は135件成功、失敗0・skip 0・終了値0。件数と全出力は機械記録および
[テスト出力](ui-illustrations-followup-test-output-2026-09-10.txt)を参照する。
基準mainのローカル129件に、長病名1件・UI表示／操作／画像失敗5件を追加。
起動329 DOM ID・100ゲーム・91コレクション、会話、開発用QA生成も対象。
保存互換性・実績等は既存の実ランタイムをNodeのDOM／時計代替で実行する。
描画性能・実物のタッチ・音の評価には使わない。

## 維持条件の照合

承認済み301画像（`9a523e46dccfd0b7d32c30ed75afc13d2549ad48`）と
基準mainの現行307画像を取得して全ファイルをバイト比較し、差分0。
初回care atlasも変更0。新規atlasの生成出力もバイト変更0。

`loadState`・`saveState`・`tick`・`isImmortal`・`triggerDeath`・`updateDyingWarning`・
`raiseDeathMeter`・`recoverSleepStep`・`renderHomeCast` の本文は基準mainと一致。
SHOP_ITEMS／NAOTO_ITEMS／FUN_ITEMSの定義と、お世話ハンドラー・会話を含む末尾も一致。
マスター、cast-layout／cast-motion、games、audio、world-environment、既存レイアウトCSSは変更0。
主人公248形態への目口貼り付け、主人公／恋人／装備サイズや左右の仲間の弧の再計算はしていない。

新atlasの全36フレームの境界・CSS座標・外周の不透明画素0を照合。
[静的縮小見本](ui-illustration-size-check-2026-09-10.png)で全種を目視した。
画像の配色・透明度・プロンプトは[素材記録](../art/world-items-icons-v1-2026-09-10.md)を参照。

## 実画面の限界と残件

公式ブラウザーは初期化できたが、タブ一覧が20秒でタイムアウト。
文書化された復旧手順後に新タブを作れたものの、公開ゲームへの遷移・DOM取得も
`CDP operation refresh tabs timed out after 20000ms` で失敗。今回のゲーム画面取得は0件。
別のブラウザー経路で成功したことにせず、以下を未確認として残す。

| 確認対象 | 今回の証拠 | 残る実画面確認 |
| --- | --- | --- |
| 320px／390px・文字拡大 | QA幅選択とcare_largeを用意、固定領域のCSSを保持 | 320×640／390×844、アプリ文字大・ブラウザー200%の欠け／スクロール |
| 26仲間と恋人・装備 | 配置計算の回帰、26＋恋人＋全商品セーブの実ロード成功 | 弧・重なり・実寸・タッチ位置 |
| 長い病名 | 3健康状態で「くすり」が先、通常病気専用セーブ追加 | 2行内で操作案内が読めるか |
| 各配色・天候 | 既存配色／環境テスト、選択時の文字と絵の対応 | 全配色・昼夜・4天候で輪郭／文字が読めるか |
| 通知の出入り・結果と増減 | 既存持続／節目／結果回帰、ボタン矩形計測を追加 | キャラ・ボタン位置不変、原文と増減の読みやすさ |
| 画像読込失敗 | errorイベント処理と元の絵文字／状態保持の回帰、QA失敗切替 | 実ブラウザーで代替表示とラベルを視認できるか |
| 動きを減らす・軽量モード | 既存警告文字の回帰、新規連続アニメーション0 | 静止状態でも意味と対象操作を識別できるか |
| iPhone描画・タッチ・音・FPS | 新規実機証拠なし | 未確認。がたつきの原因も未確定 |

実際の開発用ルートは `/__qa`（アンダースコア2個）。公開Pagesには配信しない。
既存care*12種を保ち、`care_sick_only` と `ui_illustrations` を追加した。
後者は26仲間＋恋人、装備15種、4ごほうび、7小物を持つ。
`Simulate missing icon image` を入れてから再ロードすると、CSS画像と失敗検知imgを404へ向ける。
`iconImages` のpending中は成功にせず、再計測してloaded/failedを確認する。
意図した画像失敗時の `checksPass=false` は正常で、レイアウト値と代替表示を別に見る。
`careNoticeHeight`・`careNoticeOverflow`・`careRecommended`・`careButtonBounds`を前後で比較する。

通常デート・特別／深海デート・終了ボタンの既存OKと、記念日の「ひとまずOK」を保持する。
#183公開済み、#204は#205へ統合済み。古いBS・BP・BOへ戻さず、同じ確認をユーザーへ依頼しない。
