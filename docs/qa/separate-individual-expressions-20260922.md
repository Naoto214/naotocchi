# 複数顔・別個体の追加監査（2026-09-22）

## 基準と開始確認

基準HEADは `94affc26450971c8f3532af83e2e20ac91884b07`、treeは `3f201f23321b647276889906c38201a167d38b72`。前回の同一身体81PNG修正を含む21系統1680表情を継承。GitHub refと取得したローカルHEAD/treeの一致、作業ツリーcleanを実確認した。PR278はDraft/open/未マージ。実mainはAPI `git/ref/heads/main` と `git ls-remote` とも `9f88953ef04cd288298fa9820dfeff38cbe7148e`。mainは取り込んでいない。開始時CIはActions/statuses/check-runs各0、combined pending。GitHub CI成功とは扱わない。

## 監査結果

全21系統の元画像8段階ずつ、計168枚を主担当と独立担当が確認。別個体の対象3段階を個別等倍・6倍で確認し、各10表情・計30枚を独立担当が単独画像で確認、主担当も一覧と元画像を照合した。全1680表情を単独で再目視したという意味ではない。

| 系統・段階 | 主個体 | 別個体 | 判定 |
|---|---|---|---|
| woman08 | おばあさん | 抱えた猫1匹 | 10表情保持。猫は静かな開眼・平常顔で身体状態を同期しない。 |
| clownfish05 | 大きい魚 | 小魚2匹 | 10表情保持。小魚の穏やかな通常顔は主魚の病気・疲労・危険を複製しない。 |
| dandelion08 | 中央左の種 | 共通茎のない周囲5種 | 10表情保持。不調時にも明るい顔が残るが、全種病気／疲労／危険化ではない。 |

同一身体は coral06–08、sakura05–07、venus_flytrap04/05/07/08。既存分類と前回修正を維持する。man02の衣服図柄・玩具、woman02のぬいぐるみ、hermit_crab03の空貝殻、butterfly06/cicada05の空殻は生きた別個体ではない。全系統の分類理由と30表情別判定は同日reviewを参照。未確定分類なし。

必要なPNG修正集合は空。再生成0枚。タンポポのsick/weak/criticalで周囲が明るいという見え方は残り、「全員が心配している」とは扱わない。許可された心配表現を必須化せず、微小な好みだけで再生成しないという方針で保持する。dandelion08-sulkyの種脇の短い暗色線は既存の生成差として独立レビューに記録し、今回追加した状態マークとは扱わない。

正本に同一身体の状態共有／別個体の状態非同期と自然な反応／曖昧例の照会を明文化した。10カテゴリを維持し、PNG・配置・ゲームコード・キャッシュは変更しない。確認Siteは別個体30表情と既存同一身体100表情を切り替えて確認できるようにする。画像版は変更せず、選択・メモ・コピー保存を維持する。

## 検証・公開・保存

実行結果は以下に追記する。現段階では全体テスト・公開・今回のGitHub保存の完了を主張しない。

### 最終検証結果

- 必要PNG修正0、生成0。前回の同一身体81修正を含む1680使用表情と通常キャラクター297PNGを保持。Git保存基準のblob SHAと、docs以外の全2180追跡ファイルの実内容が一致した。配置168段階、既存81位置修正、全マーク/汗、ゲームコード/セーブ関連もこれに含む。
- 全1680assetFor参照が実PNGへ接続し、全1680accentForがSVGを返す。Siteの全1680PNGのSHA256が制作リポジトリと一致。別個体対象30PNGのSHA256・維持判定をmanifestに保存。
- 配置検査:1680マーク / 1008汗動作範囲 / issues0、exit0。配置の再計算・例外追加なし。
- 最終全体npm test:1474 tests /1474 pass /fail0 /cancelled0 /skipped0 /todo0、271144.058101ms、exit0。今回初回成功、再実行なし。接続/表情/既存ゲーム回帰を含む。テスト後にランタイム・PNGの変更なし。
- 独立Spec PASS / Code quality PASS、未解決critical/important/minor0。Site説明の身体状態と感情を混同し得る文言は修正受理済み。
- Siteの実JSを独立VM/DOM模擬で評価:別個体30、同一身体100、全1680、初期woman08の10件、系統/範囲/段階移動、選択・既存メモ・コピー出力・ダイアログを確認。実機タップ検証とは区別する。
- 3段階の既存日本語付き合成一覧を主担当が目視。30マーク付き画像に新規の仲間用マーク追加なし、日本語欠字なし。一覧は静止合成、汗は近似で実機撮影ではない。
- ゲームJS変更なしのためnpm run bumpは実行不要。既存 `pet-expression.js?v=20260922-6348f2d6` を保持し、最終JSのSHA1先頭8桁一致を再確認。Siteのゲーム埋込も同じ識別子。画像版 `mf-0660b59a` と選択保存KEYを保持し、変更した一覧JSのqueryだけ `separate-20260922` に更新。

### 確認Site公開

同じowner-private Siteのversion60へ公開、status succeededを確認した。共有先はowner1名のみ、group/外部招待0を再確認。URL: https://naotocchi-emotion-pr275.kerzion214.chatgpt.site/mark-review/ 。ゲームの1680表情と画像版mf-0660b59aは維持。別個体3段階/同一身体10段階/全段階の切替と従来の選択・メモ・コピーを利用できる。

- Site source HEAD: `0ec9be3f27454e691786f8cdb100ad565c44861f`（push後に完全SHAを実取得）。
- version ID: `appgprj_6aa908e9357c8191abb0f486be58697c~appgver_a80d6a9f23048191b61dbad6ed15ab41`。
- deployment ID: `appgdep_6ab2303e9f1c81918327fc3ec91d4562`。
- 初回アーカイブのgzip末尾欠損を独立検査で検出し、保存/公開には使わなかった。再パッケージ版を同じ実行内と別実行でgzip検証し、SHA256 `438c2510a44f65d2149fedf683aaf04fe1cd8da54aaf57fdaa40eb93ff247206` が一致。hosting設定、index、対象3一覧を含む全2259ファイルの実内容一致も検査してから保存/公開した。

GitHub保存直前refは基準94affcのまま、実mainは9f88953のまま。今回の最終GitHub HEAD/tree・保存後PR状態とCIはコミット後に取得し、既存PR本文を保持して「別個体追加監査・最終保存」に追記する。自己参照SHAを本コミットへ埋め込まない。完成数は21系統1680表情のまま、未制作系統は残っており、次系統は未着手。
