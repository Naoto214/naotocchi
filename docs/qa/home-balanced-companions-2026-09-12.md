# ホームの主役・仲間の左右バランス（2026-09-12）

対象: #252 マージ後の main `46e3fef340b20006340fbad24e432462891404c1`。
開始時の tree `62bce47a99072539700ef74bd98c4fdd2717dc20` とローカル全794ファイルを照合した。

## 原因と修正

以前は右下のうんち用領域を core の幅に含め、その外側へ仲間を配置していた。
そのため、うんちが出ていないときも右翼が遠ざかり、主役も左へ寄る条件があった。

- 主役の透明余白を除いた描画範囲の中心を、フィールドの中央に固定。
- 左右の仲間は、主役の描画範囲から各画像の内側の縁までの距離を揃える。PNGの枠の中心を機械的に反転するだけにはしない。
- 対になる仲間は同じ高さ・カーブ・列を使用。異なる画像幅は対ごとの大きい側で間隔を確保する。奇数人数の最後の1人も、既存の対の配置を片側だけ変えない。
- 恋人・装備・ハートの広い側に必要な余白を、左右へ等しく確保。うんち領域を仲間の横配置計算から除外した。
- 追加指示「縮尺は変えてよいが、場所・並びは常に固定」に合わせ、うんちは主役の右下・吹き出しの上の専用の一段に固定。先頭はフィールド中央から右へ12px、全4枠の下端は吹き出し上端から2px上。人数・種類・装備・画像読込失敗が変わっても、同じ画面寸法ではこの基準位置を変えない。
- 初版の空き場所探索と吹き出し下への切り替えを削除。専用の一段はうんち0個のときも確保し、その上の共通領域へキャストを左右同条件で収める。うんちのために片側の仲間を移動させない。画面全体の高さやステータス位置は伸ばさない。
- うんちは実際の主役フレーム倍率 `main.w / 104` に連動する従来の8〜24px、間隔2px、最大4個を維持。常に4枠分を計算し、出現・掃除・発言者変更でキャラや行が跳ねない。
- 吹き出しは主役中央・ステージ下端から10px上の定位置。専用の一段を含め、主役足元からの間隔は14〜30pxとなる。しっぽは固定のうんち行との接触を避け、本人の発言では中央を維持する。
- 最小画面の8人前後、および透明余白の異なる画像と読込失敗画像が混在する30〜32人で、従来の列数制限では画面が伸びる条件を再現。通常のカーブで収まらない場合のみ短い列を追加し、最小サイズを保持する。2行の列も内側へ曲がる配置とした。
- 固定の一段を追加した最小画面では、画像がすべて読めない32人と、一部が読めない31人での拡大も回帰テスト化。多数時の揺れ幅に必要な上余白を確保しつつ、最後の候補に左右共通の浅いカーブを追加。主役32px以上・仲間12px以上を維持する。

保存データ・ゲーム進行・仲間の種類や人数・指輪のサイズ規則は変更していない。

## 検証

- `npm test`: 350件成功、失敗・skipなし。
- `node tests/home-floor-audit.cjs`: 48,306条件成功。248成長画像、18恋人、仲間0/1/2/3/6/17/18/25/26/32人、装備、画像読込失敗。主役中心誤差0px、左右の描画距離差は浮動小数点誤差のみ、固定アンカーの誤差0px。うんち8〜24px、最大102×24pxの一列。会話との間隔は専用の一段を含む14〜30px。
- 人数0〜32を1人刻みで、幅270/358px・高さ152px・恋人と装備の有無を独立に確認。8人での画面伸長、32人での一部画像読込失敗を回帰テストにした。
- 独立レビューの固定シード監査: 最終ソース `37c23098e7cb` で3,300条件成功。画像の順序変更、読込失敗混在、人数0〜32、幅270〜500px、高さ152〜340px。仲間同士・主役・恋人・装備・うんちとの接触、左右差、画面外、うんち枠欠落、固定位置からの移動なし。重要指摘は解消済み。
- ローカルChromium: 会話46条件・298状態成功。各話者、長文スクロール、掃除、揺れ、画像読込失敗、320×568、大文字、Safe Areaを確認。
- 添付に近い51歳のねこ・ゴリラ・仲間6人・花・夜のジャングル、少人数、8人、25/26人、画像読込失敗混在を実描画で目視確認。キャラ・会話・うんち・メーター・操作ボタンの重なりなし。

最終コミットのChromium／WebKit実行結果とスクリーンショットは、PRの `Home layout` チェックおよび同名artifactを参照。物理iPhoneでの確認は未実施。
