# コメント内イラスト QA（2026-09-10）

## 検証対象

基準main `0ce7175923ee577c784560229bffb5fbce9ba0dd`。
ローカルコードcommit `e704dfbe9ec1df3889a1c6118857f59e05c08689`、tree `a873f2bdd54c45a2bb825bd4d93c0c376f4a9bb2`。
GitHubコードcommit `8a67769b5939c0af75395c757c911e2bae5b2865` はこのローカル検証treeと一致。
最終保存HEAD／treeとPR CIは最新PR本文に記録する。文書自身のSHAを文書に埋め込まない。

## 自動検証

`npm test`: **161成功、失敗0、skip 0**。起動329 DOM ID、100ゲーム、91コレクション、既存会話・デート・記念日の回帰も成功。

新規9テストは実コードをNodeで実行し、以下を確認した。

- 任意文字列のHTMLエスケープ、同義atlas、コインの読み上げラベル。
- 結合絵文字・肌色・異体字セレクター。未対応の複合文字は途中で分割しない。おにぎり・食材の卵は未対応のまま保持。
- 本人、26仲間、18恋人の固有PNG。初遭遇のチョウチンアンコウをサケにしない。
- 通知の同一本文でスクロール維持、変更時は先頭、健康0のcritical優先度維持。
- イベント／誕生日／字幕の共通経路、12SVG記号の接続。
- 後から追加されたコメントPNGの失敗が、その枠以外のキャスト配置・保存状態を変えない。
- 動きを減らす設定＋軽量モードでも、本人のリアクション本文とPNGを保持。

初回は未実装で6失敗、記号追加前・リアクション追加前の失敗も確認した。途中で見つかった、通知効果の空白まで詰める問題と、同じ字幕を新たな表示として出さない問題は修正済み。
旧対話テストの初遭遇画像fixtureは、既存の達成通知による上書きを避けるため実績を獲得済みにした。ゲーム側の達成通知順は変更していない。

## 不変確認

[JSON](comment-illustrations-2026-09-10.json)に対象一覧を記録。311既存画像と12既存ファイル、保存・死亡・季節地域参照・キャスト等19関数のバイト不変を確認。新規PNG／画像編集は0。12記号はJS内のSVG図形で、ゲームのCanvasは変更していない。

元の本文・履歴文字列は書き換えない。1emの小枠を文章に差し込み、既存の2行の高さ・スクロール・キャスト配置計算を保持する。本文の見え方が実機で同じ高さに収まることは未確認。

## 表示確認の限界

[12記号の静的見本](comment-symbols-art-check-2026-09-10.svg)を64px／16pxでSVGからラスタライズして確認した。これは素材の見本で、ゲームのスクリーンショットではない。
公式ブラウザーは `CDP operation refresh tabs timed out after 20000ms`。新しいゲーム画面は0件。iPhoneの描画・タッチ・音・FPS・がたつきは未確認。

`/__qa` の `comment_illustrations` で、次の年齢tickによる31歳の誕生日、7小物、26仲間＋恋人を再現する。320px／通常幅、文字拡大、各配色、通知の出入り、画像失敗、静止設定、軽量モードを実ブラウザーで確認すること。

## 公開確認

#229はmainへ統合済み、Runtime #305／Pages #227成功。公開HTML・本体JS・ゲームJS・UI CSS・atlas 3枚、合計7ファイルのバイト一致をJSONに保存した。今回の新しいコメント対応が公開された証拠ではない。
今回分のマージ・公開状態は、最新PRとmainのCI／Pages／実ファイルを改めて確認する。

## 独立レビュー

ローカルコードcommitとtreeを指定した読み取り専用の独立レビューを実施。Critical 0／Important 0／Minor 0、未解決の指摘0。重点9テスト、対話テスト、QA fixtureテストもレビュー側で成功。全44話者の参照画像が実在することを確認。実ブラウザー・実機の表示承認は含まない。

## 残件

専用絵のない一部の実績・地域通知の印、未対応の絵文字、ミニゲーム小物を保持。未対応や画像失敗を含めた「絵文字ゼロ」は主張しない。地域参照の修正、旧#92の評価は混在させていない。
