# ホームのテイストとパラメーターの区分（2026-09-10）

## 基準と依頼

ユーザーから「基本画面が悪くなっている。添付画像のテイストを維持し、スクロールせず見たい。基本パラメーターとそれ以外を分け、縦の比率・余白を見直してほしい」と追加指示を受けた。

GitHubを再取得し、PR #212・#213がマージ済み、最新mainが `39d1da682fdf299572472c4a2f81a2afc1dee2df` であることを確認。このmainから `fix/home-hierarchy-20260910` を作成した。未マージの#211のゲーム分離・難易度変更は含めない。

## 表示の変更

- ピンクの丸い本体と淡い緑の画面を維持し、年齢・そだち・お金を画面内へ戻した。バッジはタイトルの下、キャラ名と恋人名はキャストの上にまとめた。
- 通常の高さでは、おなか／ごきげん／げんき／けんこうを2×2へ。区切り線の下にせいちょう／おとろえ／いのち／へんしん／100さいまでを5行で並べ、ラベル・バー・値を揃えた。
- 実際の可視高さが640px以下では、基本4項目を1段、成長・一生の5項目を3＋2の段組みに切り替える。各項目のラベルを1行目、バーと値を2行目へ置く。名前は折り返し可能な横並びにし、キャストに使える高さを確保する。
- Safariの可視領域変更に追従し、高さが戻れば通常配置へ戻す。ピンチズーム中は切り替えず、拡大操作を打ち消さない。
- 9ボタンは通常3×3、可視高さ640px以下では5＋4の2段にする。2段にすることで50px分の高さを回収し、文字は必要に応じて折り返す。絵文字と文字は横並び。ごはん等は淡い緑、あそぶは濃い青緑、じゃれる／きゅうあいはピンク、たびは淡い青。最小44pxの高さを保持。
- ゲージの最大値 `100 / 100` の幅、狭幅用CSSの優先順位、バッジの負の余白を修正。バッジの説明がゲームやメニューの上に残らないようにした。
- 色・柄の定義、チケット・解放条件、キャラ画像、卵の孵化、同期揺れと反応、世界・位置情報、保存・ゲームロジックを保持。JSの製品差分は高さに応じたクラス切り替えのみ。

## 自動検証

- 追加した短い可視領域のテストが修正前に失敗することを確認。修正後は高さ664→548→700の配置切り替え、ズーム中の維持、innerHeightへのフォールバックが成功。
- 修正後の `npm test` を最後まで実行し、Nodeテスト73件・失敗0・スキップ0。起動312 DOM参照／100ゲーム、会話・デート・記念日・旧キャラ互換・人生記録、QA fixtureも成功。
- 既存の数値配置テストは幅270/294/314/354/384、キャスト高さ96/120/160/220/300、仲間0/6/18/26/28人と恋人・装備、全枠・同期揺れ・反応余白を確認する。ホーム全体のブラウザー測定ではない。
- 色40種・柄40種の独立選択、見本、保存IDのフォールバックと画面の数値コントラスト試験も成功。
- `git diff --check` 成功。GitHub上の最終HEAD・PR CI・統合後Runtime・Pagesの確定状態は、このブランチのPR本文とActionsで確認する。
- 独立レビューで、ゲージの区分変更だけでは375×548の通常のお世話応答で高さ不足になることを指摘された。低い画面の9ボタンを5＋4の2段へ修正し、ローカル製品commit `e2a3eac122c23dfc25ebfce89b132b28ded0d609` を再レビュー。Critical／Important／Minorの残件なし。通常の1行応答の最小高さはコードから約520pxと見積もられ、元の確定的な高さ不足は解消。これは実ブラウザーでの実測値ではない。

## 確認の限界

公式ブラウザーは今回も `CDP operation refresh tabs timed out after 20000ms` で実画面取得に失敗した。別ブラウザーや制限回避は使用していない。

320〜390px幅、文字サイズ大、長いセリフ・名前、バッジが多い状態、追加操作が出る状態をCSS/DOM構造で点検したが、iPhoneでの文字幅・実際のスクロール量・safe area・タッチ・滑らかさは未確認。高さ不足や大きな拡大では内容を切らずスクロール可能なままとする。通常時の全体表示を目的とした実装であり、全条件でスクロール0を実証したものではない。

会話内の配置見本は製品と同じ画像・CSS・配置計算を使った例示であり、公開ゲームやiPhoneのスクリーンショットではない。
