# 顔なし卵の実装・検証 — 2026-09-10

## 基準と今回の区切り

最新main `a26a7db2e8e9457a1d435d2875db9268bb70095c`（#208まで）から `feature/faceless-egg-20260910` に実装。作業中の再取得でもmainは同じ、既存のopen PRは#92のみだった。#207のUI／全体の揺れと#208の話者別枠線を含む。

この記録は実装候補の証拠。対象PRの最新HEAD・tree・Actions・マージ状態はGitHubで照合する。以前のチェックポイントの#205未マージ指定は履歴として扱い、mainを巻き戻さない。

## 実装内容

- [専用PNG3枚](../art/egg-128.md)を追加。顔なし、クリーム色の殻、青銀色の模様。
- 既存のgrowthが40%で小ひび、80%で大ひび。あたため1回4、孵化しきい値20は維持。
- あたため時560msの殻の反応、孵化時900msで上下の殻が離れて決定済みの乳児が現れる。親の `cast-sway` と `petSprite` の動きを上書きせず、内側の要素だけに有限アニメーションを適用。
- 孵化の実状態と保存は既存どおり即時確定。演出を保存せず、演出待ちの入力ロックやゲーム状態を変える遅延タイマーを追加しない。
- `prefers-reduced-motion: reduce` では孵化殻を非表示、卵の反応と乳児の登場アニメーションを無効にする。画像エラーは既存の絵文字fallbackを使用。
- `/__qa` に `egg` (growth0)、`egg_cracking` (8)、`egg_ready` (16)を用意した。製品側に確認用セーブや時計変更は入れない。

## 自動検証と素材点検

変更前の `npm test` が成功（従来44件＋runtime/dialogue/QA fixture）。追加した表示テストは変更前に3件失敗し、専用PNGがまだなく絵文字を返していたことを確認。その後の実装で成功。

最終の `npm test` は **51件成功、失敗0**。これとは別に既存のruntime、dialogue、QA fixtureの各プログラムが成功。GitHubでも卵のテストを実行するようworkflowへ追加した。

新規7テストの範囲：

1. 5回の連続あたため、段階ごとのPNG、孵化境界、出生記録1件、重複renderで殻を増やさない。
2. 0〜4回あたためた全5保存点からの再読込。growthに対応する卵を表示し、操作演出は再生しない。
3. 5回目直後の保存・再読込で同じ種族／性別／指向と出生記録1件を保持。
4. 孵化演出中にテスト用の完全リセットを実行しても、新しい人生へ演出や孵化が遅れて流入しない。
5. 卵そのもののタップと、孵化直後のお世話操作。
6. 「次のたまご」の全31種族が安定IDと各01.pngを保持して孵化。
7. 壊れた卵画像の実error handlerでfallbackを選び、育成・孵化を続行。

新しい3つのQA fixtureのgrowth、卵状態、仲間0・恋人なしも検査した。従来のミニゲーム100本、会話・全デート、記念日、保存復旧、旧キャラ移行の検証は既存スイートで継続する。

素材は3PNGをそれぞれ目視。全て128×128／RGBA／二値alpha／63色以下／余白8px以上／接地点y=119。基準mainのassets全301ファイルをgit blob hashで照合し、変更なし。[測定値](faceless-egg-assets-2026-09-10.json)を保存した。世界マスター、`style.css`、`ui.css`、保存形式と孵化の種族抽選は変更していない。

## ブラウザーと後日の実画面確認

既存のクラウドブラウザーへ接続を再試行したが、タブ一覧の取得で `CDP operation refresh tabs timed out after 20000ms`。ゲーム画面の取得は0件で、復旧したとは扱わない。別の接続方式で迂回せず、ユーザーの「実装後に確認」に従って実装・記録を先へ進める。

後日、開発環境の `/__qa` で次を確認する：

- `egg`：あたためるボタン／卵タップ、2回目の小ひび、4回目の大ひび、5回目の殻の開きと子の表示。320／390px幅で上下左右の欠け・位置飛び・二重像を見る。
- `egg_cracking`／`egg_ready`：開始段階と再読込、最後の1回から孵化まで。素早い連打と孵化直後のお世話。
- OSの動きを減らす設定、画像が読めない場合、実機Safariでの殻の切り分けと描画速度。

Node検証は実機の描画・FPSの合格には数えない。過去に受領した通常／特別／深海デートの確認と記念日の暫定OKを取り消さない。既報のがたつきは原因未確定の残件であり、この演出の実装で修正済みにしない。伝説の残る9物語など全体の未確認範囲も引き続き残る。
