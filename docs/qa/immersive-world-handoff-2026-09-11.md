# 世界一体型ホーム — 中断時の引き継ぎ

## 現在の状態

2026-09-11。ユーザーが別タブへの引き継ぎを希望したため、実装途中の状態を保存する。完成・マージ可能とは扱わない。最新main確認は `d2a350aa4f83fb7aac36c19accb7e8179fc4e393`（#238）、この作業以外のopen PRは旧#92のみだった。再開時には必ず再確認する。

ブランチ: `feature/immersive-world-20260911`。このファイルを含む最新コミットを正とし、PRのHEAD/tree/CIを実際に取得する。通常git pushはローカル認証未設定で失敗したため、接続済みGitHubのblob/tree/commit/branch APIで同じファイルを保存した。各blobと全treeをローカルgitと照合する。

## 実装済み

- 本体/液晶枠の廃止、全画面の世界、海で先に試作した後に13地域へ展開。
- 世界画像・前景・環境とキャラ/会話/HUDの分離。画像32枚、時間/季節/地域ごとの気候、共通表示専用レンダラー。
- 海中は雨粒/雪なし、深海/星空は独立照明、熱帯/砂漠は雪指定でも積雪なし。森林/複数の地上地域に冬の別画像。
- 元のゲーム/保存schema5/100ゲーム/キャラ配置/反応/装備/恋人/仲間/うんち/会話/イベントを保持。旧テーマ/柄はメーターとボタンの控えめな装飾として保存IDと解禁を維持。
- 命の4段階、危険時のお世話案内、推奨操作、恋人のなかよし度。ゲーム/物語/メニュー/非表示で世界の動作を停止。

## 最後の追加指示と未完了箇所

ユーザー: 「ボタンやパラメータ、バッジや名前、上下の表示されてる部分などは、半透明にして、奥の景色が透けて見えるようにしてください。ただ文字やメーターの視認性は維持してください」。

この指示に合わせ `world-scene.css` を追加修正。従来の白い面の約88–95%不透明を、通常60%、旧テーマを合成した面61.6%、意味のある色64–74%へ下げ、ぼかし9px→3px。文字/数値/アイコン/ゲージ本体は薄くせず、濃い文字色、ゲージの明るい軌道と輪郭で区別。上部タイトル/データ/つうしん/年齢/環境/名前/バッジ面/下部メーター/操作/会話/通知にも適用。

**未完了（優先順）:**

1. 海の実ブラウザーで `.header-title` 等は `rgba(245,253,255,.6)`、メーター/一般操作はalpha .616、blur3px、文字opacity1を確認。**「たび」`#travelBtn` は背景 `rgb(227,237,245)`・文字 `rgb(72,107,138)`、「メニュー」`#menuBtn` は背景 `rgb(255,250,243)`・文字 `rgb(119,96,144)` が残る。既存CSSの優先度を調べて半透明へ統一する。** index.htmlにinline styleはない。
2. 通知の critical と loss/mixed のCSS優先順位、旧テーマの暗い装飾を含む文字/ゲージのコントラストを確認する。全要素のopacityを下げる方法にはしない。
3. 半透明変更後は海390×844の配置成功まで。昼/夜、森/みずべ、危険/通常、バッジあり、旧せいざ/レインボー、320×640満員と文字拡大を再確認する。追加した名前/命表示のborderによる高さの変化も確認。
4. **現在のdocs/qa/immersive-world-imagesの6枚は半透明追加指示の前の画面**。新しいものを撮影して差し替え、QA記録/PR本文を更新する。
5. `npm run bump`、必要なテスト、レビュー、最新main/open PR確認を行い、PRの最新HEADのCIを確認してDraftを解除できる状態まで進める。ユーザーの確認なくマージ・公開しない。

## 検証の範囲

半透明変更前: 全272件成功。13地域×4時間×4季節×4天気=832組のモデル検証。クラウド実ブラウザー23シーン配置成功、じゃれる/きゅうあい、夕焼けデート完了/復帰、ライツアウト開始/中断/復帰を確認。独立レビューの問題は修正済み。

半透明追加変更後: `npm run bump`でindexのCSSトークン更新済み、`git diff --check`成功、`npm test` **272件成功/0失敗**（約9.46秒）。海390×844の再計測 `layoutChecksPass: true`。その他の再撮影と視認性の最終確認は未完了。

物理iPhoneのSafari/VoiceOver/低電力FPS、全100ゲームの今回の実画面検証は行っていない。832組はモデル検証であり832枚の撮影ではない。

## 読むべき資料と実装

- `docs/superpowers/specs/2026-09-11-immersive-world.md`
- `docs/superpowers/plans/2026-09-11-immersive-world.md`
- `docs/qa/immersive-world-2026-09-11.md`（追加指示前の詳しい検証）
- `docs/qa/immersive-world-browser-2026-09-11.json`（同23シーン実測）
- `assets/world/provenance.json`（プロンプト、生成参照、32素材ハッシュ）
- `world-scene.js`, `world-scene.css`, `tests/world-scene-test.cjs`
- `tests/visual-qa.cjs`（`world_sea` 等の合成セーブ。実セーブを変更しない）

ローカル作業場所は `/workspace/scratch/ab4cb3209e09/naotocchi`。次のタブで存在しない場合はGitHubの作業ブランチを取得する。ローカルの旧コミットSHA `0ec4edf10fd323c29bca4086e3deddde70fa5abe` は最初の検証版で、最新保存地点ではない。

今回の目標は「デジタルペットの本体」から「小さな世界でなおとっちたちと暮らす」への変更。旅・地域・仲間・恋人・シール・将来のカードを自然に載せられる構造を守り、薄い背景の差し替えだけに戻さない。
