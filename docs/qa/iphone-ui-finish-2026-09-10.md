# 公開UIのiPhone向け仕上げ点検 — 2026-09-10

## 基準と結論

開始時と修正後の照合時に、main は `1b8192fddd833eb24a215b4ab6009f875bb3091f`、tree は `e875757eb0659018b6967c2d2141f39724532157`。PR #207 はマージ済みで、その後の追加コミット／PRはなかった。既存の未マージPR #92には触れていない。

PR #207の [Runtime](https://github.com/Naoto214/naotocchi/actions/runs/34428337038) と [Pages](https://github.com/Naoto214/naotocchi/actions/runs/34428336634) は成功を再確認した。[承認仕様](../superpowers/plans/2026-09-10-ui-integration.md)、[統合検証](ui-integration-2026-09-10.md)、[公開後チェックポイント](../../POST_RELEASE_CHECKPOINT_2026-09-10.md)を読んでから点検した。

**再現したCSSの競合1件を修正。iPhone／Safariの実画面、タッチ、滑らかさは未確認。** 以下の自動検証・ソース点検・API応答確認を、実機確認の代わりには扱わない。

## 修正した問題

`style.css` の `.speech-bubble[data-kind='partner']` は二重線、`companion` は破線だったが、後から読み込む `ui.css` の `.speech-slot .speech-bubble` が `border:2px solid #63735b` で上書きしていた。双方の詳細度は `(0,2,0)` なので、恋人／仲間の枠線が実線に上書きされていた。

共通UIでは枠の幅と色だけを指定するよう変更し、既存の話者別 `border-style` を保持した。再読込時に新CSSが選ばれるよう `index.html` の参照を `ui.css?v=20260910-ui-2` に更新した。

ソースから該当セレクター・宣言順・詳細度を評価する限定的な検査で、変更前は恋人／仲間の2ケースが不一致、変更後は以下の3ケースが一致した。これはブラウザーのcomputed styleや描画を取得した結果ではない。

| 話者 | 変更前の指定結果 | 変更後の指定結果 |
| --- | --- | --- |
| メイン | 実線 | 実線 |
| 恋人 | 実線 | 二重線 |
| 仲間 | 実線 | 破線 |

## 今回確認できた範囲

`npm test` を変更前と変更後に実行し、最終実行は exit 0。既存の起動・会話・デート・記念日・伝説・QA fixture と、Node test runner の44件がすべて成功した。テスト内のDOM・時計・位置情報許可は代替実装である。

| 対象 | 今回の根拠・確認範囲 |
| --- | --- |
| 同期する揺れ | ソースで、共通の `castSway` 親要素による3.2秒／左右4pxと、個別キャストのアニメーション上書きを確認。キャスト再描画のキャッシュとreduced-motion時の静止を保持 |
| 仲間26人と旧セーブ | 既存テストで13＋13のDOM生成と旧キャラ互換を確認。キャスト領域幅270／294／314／334／354／384px、0／6／18／26／28人の枠・揺れ余白・仲間間4pxの計算検証に成功。全形態・全恋人の実画面組み合わせを網羅した検証ではない |
| 恋人・装備・名前 | 配置計算と元画像を維持。名前横ハートは `1em`。上部の名前と下部の吹き出しのDOM構造を確認 |
| お世話・ゲージ | きゅうあい連打、健康時の薬と反応、あそぶ即ランダム開始の既存回帰が成功。9ボタンと9ゲージ、おとろえ・いのち・せいちょう・へんしん・100さいまでを保持 |
| メニュー・デザイン・通信・デート | メニューの時計停止／排他、直接旅、ゲーム／実績の振り分けと既存デート回帰が成功。色40／柄40と解放条件、字体・文字サイズ、通信説明、恋人データ内のデート導線はソース点検。組み合わせの見栄えは未確認 |
| じかん・きせつ・てんき | 5択の構造と端末時計の参照を点検。手動天気の保持、非同期応答のメニュー割り込み防止、拒否後の明示的な再試行、成功後の更新、睡眠中の現在地表示保持の回帰が成功 |
| 市区町村・天気の実応答 | [HeartRailsの公式サンプル](https://geoapi.heartrails.com/api.html)にある経度135.0・緯度35.0へのJSON応答を取得し、製品パーサーで `{name: '西脇市', kana: 'にしわきし', display: 'にしわきし'}` に限定できた。同じ公開サンプル地点のOpen-Meteo応答 `weather_code: 1` は製品パーサーで「はれ」に変換できた。ユーザーの現在地・住所を送信した検証ではない |
| 保存・画像 | 保存復旧10件と旧キャラクター回帰が成功。`assets/`、WORLD_MASTER、`script.js`、`style.css`、キャスト／環境JS、依存関係は基準mainとの差分なし |

## 独立レビュー

製品差分・CSSカスケード・承認仕様との整合を独立レビューし、Critical／Importantの指摘はなかった。検証記録の不具合説明1か所を明確化した。レビューの対象はこの限定的な修正と記録であり、iPhoneの実画面合格ではない。

## 未確認の範囲と再開点

クラウドブラウザーは初期接続とタブ作成まで進んだが、公開URLへの移動で `CDP operation refresh tabs timed out after 20000ms`、その後の画面取得で `CDP operation refresh tabs was superseded by browser recovery`。今回取得できたゲーム画面・スクリーンショットは0件。サイトの障害やbotブロックと断定しない。別ブラウザー、ローカルURL、data URL、代理配信等で制限を回避した検証は行っていない。

次回は修正PRと最新mainを照合し、実際に接続できるiPhone／Safariで以下を確認する。これらの合格を今回のテスト成功から推定しない。

- 仲間26人・恋人・装備の全身、間隔、同期する揺れと体感の滑らかさ。
- 狭幅、横向き、iOSの文字拡大とゲーム内「大きめ」、長いセリフ時のゲージ・下部9ボタン。特に5文字の「あたためる」「きゅうあい」が狭いボタンに収まるか。現時点では崩れを実画面で再現できておらず、推測によるレイアウト変更はしていない。
- 指でのタップ、スクロール、片手での届きやすさ、ホームインジケーター／Safariのバーとの干渉。
- 色・柄・字体の組み合わせ、各メニューと恋人データからデートへの見え方、閉じた後の復帰。
- 実際の位置情報許可・拒否・再試行、実際の現在地での市区町村表示と天気。外部APIのサンプル成功は、Safariの許可画面・CORS・境界付近の精度を検証したものではない。

既にユーザーから受領したデートのOKと記念日の暫定OKを保持する。同じ手動確認を今回の修正の統合条件へ戻さない。
