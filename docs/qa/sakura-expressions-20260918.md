# サクラ表情 制作・検証記録

## 開始時の実確認

2026-09-18から19にかけて、承認済み一括制作方針を継続。完成済み19系統1520表情は再生成せず、資料と元画像を確認して未制作のサクラを選定した。全キャラクターの完成とは扱わない。

GitHub HEADは`de5b7cb9bf9605b78559fc1992c13b3dae0d15c6`、treeは`66f6bc23fb0a37c1328e9c45ffcbf26ca4f59eed`。旧制作checkoutが存在しなかったため同じブランチを復元し、同treeの分離worktreeで制作。実mainは`ae0a1254c55ad42217890dd508750f4f63bd486d`をGitHub git/ref/heads/mainとgit ls-remoteの双方で確認。PRのbase_shaのみでは判定していない。PR #278はDraft/open/未マージ、既存競合あり。Actions・statuses・check-runs各0、combined pending。mainは取り込んでいない。

同じowner-private確認Siteのversion56とsource HEAD `304dfe108e5b91201207b622097021de4bde63ec`を確認。Siteのmainはゲーム本体のmainとは別。

## 元画像と顔

8枚すべてを等倍・6倍・元画像座標グリッドで主担当と独立担当が確認。

| 段階 | 元画像 | 編集対象 |
|---|---|---|
|01|芽ぶきのたね|斜めの種の顔|
|02|ふたばのサクラ|種の顔|
|03|小さなサクラ|幹の下部|
|04|育ったサクラ|幹の下部。元画像通りピンクの花を保持|
|05|サクラのつぼみ|中央の直立したつぼみのみ。周囲4つは保持|
|06|サクラの花|左の大きな花の黄色い中心のみ。右の花と右下の小つぼみの顔は保持|
|07|サクラの実|中央手前の実のみ。左のウインクと右の驚き顔は保持|
|08|年を重ねたサクラ|幹の下部|

顔アンカー: `[[51,85,17,100],[65,85,37,94],[64,95,49,80],[64,87,50,78],[65,69,51,79],[46,76,19,74],[58,94,36,81],[62,89,46,80]]`。06の頭幅は左花全体、表情編集は中心だけ。

## 検証の途中記録

- 変更前の全体npm test: 1405成功、失敗/cancelled/skipped/todo各0、273217.109901ms、exit0。
- 実装前RED: サクラ未対応の10件が意図通り失敗。最終テスト結果ではない。
- 対応追加後の同じ10件: 10成功、失敗0、6622.274371ms、exit0。
- 途中の既存保持検査: 1520PNG/画像参照/SVG、152段階配置、通常キャラPNG297枚（通常段階248枚）、他runtime JS24件とCSSを保持。
- 01〜05の50枚: 全単独画像を独立目視確認し、blockerなし。元画像の顔以外がピクセル完全一致する方式ではなく、輪郭・陰影等に生成差はある。

制作・後半レビュー・最終manifest・配置・全体テスト・公開・GitHub保存は現在進行中。以下に最終結果を追記するまで完了と扱わない。

## 画像・配置の最終結果

全80枚の単独画像を独立担当が目視確認。06-sickの右側の花の目だけは閉じた笑い目への変化を検出し、生成元と元画像を参照して修正、再レビューで解消した。manifestにreplaces_source / repair_reason / repair_promptを保存。80件の異なる生成元、complete:true、80/80、240SHA256照合、128×128・alpha・元画像bounds80件一致を確認。未完了checkpointは最終成果から除去。

新8段階だけ配置。既存152段階の位置、過去81修正を保持。アルゴリズム変更やサクラ追加例外は不要。既存サンゴ/antlion02例外を維持。最終検査は1600マーク/960汗動作範囲、issues空。衝突0は理想的な見た目の保証とは区別する。枝・花・葉・周囲の顔を避けるため、マークは外側・頭上寄り。特に05〜07の複数顔では、主顔基準でも周囲のキャラの上へ出る制約がある。

主担当は日本語付き段階別8一覧で全80マーク合成と欠字なしを確認。これはPNG/SVG/CSS静止合成で、汗は近似。実機撮影・実機クリック確認ではない。03/04/06など小さい顔では弱い・疲れた等が近く見えるが、確定マークと組み合わせて識別可能。

npm run bumpは37識別子の日付を更新したため、生成されたpet-expression.jsの完全トークンだけを採用し、無関係な36識別子はbaselineから保持した。最終トークンは`pet-expression.js?v=20260919-acdb6cb9`、最終JSのSHA1先頭8桁一致。index.html変更はこの1件だけ。

独立合成レビュー: 80/80 PASS。最終80PNGのSHA256が独立単独レビューの受理済みハッシュ記録に含まれることも照合。独立Spec PASS / Code quality PASS、critical/importantなし。ただしこの時点では最終全体テスト・公開・GitHub保存は未確認。後続の最終記録を参照。

準備済み確認Siteの検証: 20系統1600件、初期サクラ、今回の追加分80件、8日本語一覧と80PNG一致、既存1520PNG保持、画像版sa-957a8d12、最終JS SHA1/cache一致。タップ選択・メモ・コピーのソース/状態を検証。実機クリック確認とは区別。

## 最終全体テスト

最終80画像・8配置・キャッシュを含む版でnpm testを実行。1439成功、失敗/cancelled/skipped/todo各0、295845.118284ms（約4分56秒）、exit0。今回の最終全体実行は初回成功、再実行なし。実装前REDの10件失敗と区別する。テスト後の実装・画像変更なし。

## 確認Site公開の最終結果

同じowner-private Site version57を公開し、deployment status succeededを再確認。URL: https://naotocchi-emotion-pr275.kerzion214.chatgpt.site/mark-review/ 。初期サクラ、「今回の追加分だけ」で80件。20系統1600表情の確認版。全キャラクター完成ではない。

Site source HEAD: 740e80ad0eb8561fd3fc84a9909c7d0624db6813
画像版: sa-957a8d12
version ID: appgprj_6aa908e9357c8191abb0f486be58697c~appgver_b65c91a52bb48191969a6fa45e377c7c
deployment ID: appgdep_6aadd9999e948191915e86b3b99415ff

公開前にownerのみ・groupなし・external0を再確認。ソースcommit/push後に完全SHAを取得し、package-site helperを使用。初回アーカイブはhelper終了後の独立gzip検査で末尾欠損を検出したため使用せず、再パッケージしたものを同じコマンド内と独立コマンドで検証。最終gzip正常・SHA256 abea32885186cd0be581ffa359d13a159df569516592139dee920558fcb6f0ed・81,860,681 bytes、hosting設定/index/8一覧/80PNGを確認してsave/deployした。ゲームコードの変更やテスト再実行は不要だった。

GitHub最終保存のSHA/tree・保存後実main/CIは、この文書を含む最終commitの後でPR #278本文に追記する。PRはDraftを維持しmainにマージしない。
