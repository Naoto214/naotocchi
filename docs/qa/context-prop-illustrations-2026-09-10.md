# 通知・道路・カードのイラスト検証

## 対象と結果

base `1b812864f8e39de9e9117401c15186c4de54d9b9`。
検証コードHEAD `170c170a9c33f72ca6a0cba83627ac0ce1251880`、tree `000c43cee0a9a524f4f36d595039f5b7f4593aae`。
ローカル検証commit `cfe11b6c457c9b1bab24a34db5a10c718169d7db` とGitHub保存コードは同じtree。
ローカル `npm test` は **178成功／失敗0／skip0**。追加8件をnpmとRuntime専用ステップへ接続済み。コード保存HEADのRuntime #314（run 34497014493）は専用ステップを含め成功。
その後の記録と食品の再利用を説明するコメントだけのcommit、最終HEAD・tree・CI・統合状態は最新PR本文／コメントを参照する。

| 対象 | 検証内容 |
| --- | --- |
| 文脈 | 川・砂漠の絵、環境通知のことり、一般の鳥／フェニックス／カエル／蝶／卵を保持、保存値不変 |
| 読込 | 未読込からの復帰、異寸法atlas、drawImage例外、同画像キャッシュ、Canvas状態復帰 |
| road6登録 | 実ゲームで小物を描き、本人／操作する宇宙船・車のglyphを維持。絵の失敗時に元の記号を描画 |
| 操作と報酬 | 6登録の同じ入力で、イラスト有無の得点・メッセージ・保存値・完了時間が一致。宇宙は静止／軽量設定でも比較 |
| 神経衰弱 | 裏面に表絵を出さない。4×4・4×5を実入力で全ペア完了。8／10種類の絵を区別し、得点・回数・報酬が元表示と一致 |
| 他ゲーム | 共通roadを使う滑走・レース・GPがこの追加描画へ入らない。静止／軽量でイラストを残し、終了後は遅延描画・保存変更なし |
| モジュール欠落 | 追加JSがない場合も文字を安全にエスケープし、宇宙ゲームが元記号で完了 |

最初の新規テストは3失敗／1成功で、未実装の表示を検出。途中のテスト補強で代替Canvasが未定義属性を関数として返す問題、5行盤面の代替矩形、記録件数の打切りを修正した。これらはテスト側の不備で、本番ゲームの不具合とは扱わない。

Node上の本番コードを、DOM・時計・Canvas命令記録・明示した矩形で実行した検証。ブラウザーのSVGデコード・文字幅・CSS配置・実タッチやFPSを検証したものではない。

## 不変条件

311既存画像、master等12ファイル、保存／死亡／お世話／地域参照／キャスト等19関数、実績・環境通知・地域／季節装飾の定義はbaseと一致。
games.jsは表示関数の受渡し、makeRoadGameのopt-in、共通roadの描画fallback、神経衰弱の表絵だけの変更。その変更を除けば全バイトがbaseと一致する。元のゲームタイトル・本文・制限時間・乱数・判定・得点・安定IDは不変。

## 素材と実描画の限界

[素材見本SVG](context-prop-art-check-2026-09-10.svg)は本番の18SVGから生成し、64px／20px、暗い背景の24pxを画像へレンダーして確認した。#231の食品／通知素材見本も読み、線と配色を合わせた。既存素材の描き直しではない。

公式ブラウザーは接続・新しいタブの作成後も、タブ一覧および公開ゲームへの移動で `CDP operation refresh tabs timed out after 20000ms`。**今回のゲーム画面取得0**。新タブ作成成功を描画確認とは扱わない。

320px／通常幅、文字拡大、配色、26仲間＋恋人、画像失敗／通知出入り、静止／軽量の実描画、iPhoneタッチ・音・FPS・がたつきは未確認。既存デート／記念日のユーザー判断は取り消していない。

## 配信と並行作業

#231統合mainのRuntime #310・Pages #229成功を再取得。実配信は本体JS・master・3atlasの5/8ファイルがバイト一致。HTML・games.js・UI CSSはタイムアウト、再取得はネットワーク承認の取消しで終了。残りを一致とは主張しない。

再開時#92、保存前には#232もopen。#232の実差分には成長・睡眠・老い・経済・中年／日常イベントの変更があった。保存前確認HEAD `02eb9085e36740f6b776926a2d1fbe3e4a490953`、open、コメント0件。今回未取込。全文言修正の別ブランチは最初は未コミット差分があり、最後の読み取りでは `ea2c57c87a9ffb93de0cbd1a5b2e1c5654376bb3` に保存済み・クリーン。両作業の本文やmasterは変更していない。

## 独立レビュー・残件

独立レビューは重大0・重要0、食品の再利用範囲を説明するコードコメントの軽微な不一致1件。コメントを修正し、独立再確認で未解決のコード指摘0件（重大0・重要0・軽微0）。実行コードの変更はなく、再確認では全テストを繰り返していない。独立実行で全178件と新規8件が成功し、SVG対応29キー／4PNGの39pxデコードと14atlasキーのJSON座標一致を確認。これはブラウザー表示確認ではない。同名JSONにも記録した。最終保存HEADのCIは最新PRを参照する。全体開発、未対応の通知記号・他ミニゲーム小物、地域参照の別枠点検、実機確認は未完了。[引継ぎ](../handoff/context-prop-illustrations-2026-09-10.md)を参照。
