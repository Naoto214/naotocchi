# 地域の整合性と遊びの改善 — 2026-09-11

## 対象と再開地点

ユーザーの「みずべに変更して、監査内容の改良を進める」という指示を実装した。

- base main: `df4947085c638de9151caa65090d56e268ecd3cb`（#232・#233・#234を含む）
- branch: `feature/region-identity-20260911`
- コード検証・独立レビュー: `c4ab2c00f8ffe332288f8ddf57469af633ab2674`
- 最新の保存HEAD・CI・統合状態はPRで確認する。本書作成時点では未マージ・未公開。

## 変更

| 監査の指摘 | 改善 |
| --- | --- |
| 水辺の名前 | 表示と読み上げを「みずべ」に統一。保存用の `river_lake` は維持 |
| 山・深海・みずべ・ジャングルに別地域の飾り | 実際の地域idで装飾とキャッシュを切り替え、往復しても正しい景色にする |
| 深海に日差し・雨・雪、熱帯の冬に雪 | 水中と星空を地上の天気・季節補正から分離。ジャングル・砂漠の冬の予想に雪を混ぜない |
| 現在地の観測が遠い旅先にも使われる | 観測はおうちで使い、旅先は地域の気候から予想。地上の手動指定は保持 |
| 山と雪国が同じ滑走ゲーム | 山は岩場のぼり。雪国のスキー／スノーボードは継続 |
| 滞在メリットの弱い地域 | 雪国は睡眠回復と遊ぶ疲れ、ジャングルは出会い、砂漠はゲームのおかねを改善 |
| 特別な旅先の差が薄い | 星空は星を見送る・かけらを拾う出来事と遊ぶ疲れ軽減。湖は実際の記録を振り返る出来事と睡眠回復 |
| 伝説の地域データが抽選に使われない | 未遭遇優先の候補内で、ゆかりの地域の重みを2、それ以外を1にする |

地域カードと「いまのせかい」に実際の効果を表示する。効果の合成上限0.7〜1.5は維持しているため、他の効果が重なる条件では地域差が上限で小さくなる。

## 自動検証

`npm test`: **234成功、失敗0、skip0**。基準の214件に地域関連20件を追加した。起動smoke、会話、開発用QAセーブのチェックも成功。smokeは333 DOM ID、100ゲームの開始、91登録集合を確認するもので、全ゲームの実画面を検査したものではない。

- 同じ季節で山↔雪国、深海↔海、みずべ↔森、ジャングル↔おうちを往復し、固有の飾りに切り替わる。
- ジャングル・砂漠の1月31日×8時間帯の予想に雪がなく、深海・星空の四季に地上の予想天気を作らない。
- 現在地の雪を取得しても、旅先のジャングルには流用しない。深海の手動4天気と季節切り替えでは地上の演出・補正を適用せず、元の設定を保持する。
- 山・深海・みずべの地域ゲーム優遇へ、旧ベース地域のゲームを混ぜない。
- 雪国・ジャングル・砂漠・特別な旅先の合成効果に、意図した利点がある。
- 昼・冬・雪設定でも深海と特別な旅先は専用の出来事を選ぶ。湖の思い出は保存された文章を使用。睡眠中やメニュー中は発生させない。
- 伝説の地域補正が実際の抽選結果へ反映され、他の伝説の可能性と未遭遇優先を保つ。
- 岩場のぼりの開始猶予、タイミング成功、登頂、つかみ損ね、キーボードの岩選択、時間切れ、途中終了、終了後の描画停止と二重報酬防止。
- 岩場のCanvas描画へ有限の座標を渡す。これは描画呼び出しの検証で、文字幅や見栄えの検査ではない。
- `river_lake` のセーブ再読込、訪問歴、過去の地域名を含む思い出、`downhill-mountain` のプレイ回数・自己ベストを維持。全100ゲーム、おうちを含む通常11件を維持。

`git diff --check` 成功。既存の `assets/` 以下に変更なし。キャッシュ識別子は `npm run bump` で更新済み。

## 独立レビュー

コード全差分を確認し、Critical／Important／Minorいずれも未解決指摘なし。独立実行の地域・環境・ライフサイクル51テストが成功した。Canvasからの岩選択と、難易度3段階で違う岩を選んで登頂する追加確認も成功。セーブの互換性、伝説の未遭遇優先、終了と中断の制御を確認した。

## 実画面と残る確認

Control Browserの接続・タブ作成後、ローカルゲームへの移動が `net::ERR_BLOCKED_BY_CLIENT` で拒否された。今回の実ゲーム画面取得は0件。別の経路で制限を回避していない。

岩場のぼりの手触り、320px幅・文字拡大でのカード／新しい操作ボタン、各配色、静止・軽量設定での見栄え、iPhoneのタッチ・音・速度は未確認。既存画像の再利用とCanvasの岩を使用し、新たな画像は生成していない。

今回の対象は地域の選択・景色・環境・遊びと滞在の意味付けであり、過去の全台詞・全ゲームの小物・全マスター仕様書の再監査ではない。旧#92の整理は継続して別枠で扱う。

## 次の確認

1. PRの最終HEADとRuntime結果、最新mainとの差分を確認する。
2. ローカルでゲームを開き、旅先カードから往復、世界設定で天気を切り替える。深海と星空では元の手動設定が保持されることを見る。
3. ゲームきろくから「やまの岩場のぼり」を選び、岩の選択・つかむ操作・休憩点・登頂を確認する。
4. 特別な旅先はそだち70以降。湖の出来事は人生記録を参照し、空の記録でも破綻しない。環境の小さな出来事は待機中の抽選なので、移動直後には確約されない。
