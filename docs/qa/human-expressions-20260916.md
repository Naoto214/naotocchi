# Human expressions — 2026-09-16 — verified

Baseline PR278 HEAD3791f81323a2a163ec38a861aa103c7caeddfe92; main3f4bfda0b8c0d30098ebb68c4313abd370a8576a. Both baseline GitHub workflows passed. User approved man/woman, all8 stages,10 states each, with autonomous comparison-sheet review and positioning. Keep Draft/no merge.

## Connection and review

- All16 human stages now route the existing10 state/reaction names to individual PNGs. Original normal assets unchanged; malformed and unsupported assets keep fallback.
- Human hunger uses pale rice above a yellow bowl, with the same shared outlines/colors. Head offsets vary by stage; clouds/arrows/Zzz receive shape-specific clearance.
- All16 human forms added to disposable preview, memory-only state and pre-unlocked age milestones. No normal-save access, no automatic care clicks.
- New20 resolver/runtime/preview tests failed before implementation. New16 PNG tests also failed on missing support/assets as expected.
- Independent code review found no actionable findings;109 connection/preview tests passed. Exact160 old cat/dog asset paths and complete SVG strings unchanged.
- Final PNG suite:34 passed,0 failed. Final npm test:792 passed,0 failed,0 skipped; exit0. All160 final portrait-plus-mark composites visually reviewed. Cache token updated; git diff --check passed.

## Art workflow

Each PNG is an independent built-in image_gen edit of its own original. Hair, clothing, pose and carried props retained; only human face should change. No baked-in state symbols/text. Transparent output normalized with ImageMagick to original128px bounds and RGBA8. Full generation manifest retained during work; final QA will record hashes and reviewed outputs. Man01 weak/critical sources reassigned after severity review; woman05 tired/critical sources reassigned so yawning is tired and the exhausted small-mouth face is critical. A late-male recovery mapping error selected wrong-stage sprites; the first10 mappings were replaced using individually inspected exact raw filenames. Root reviewed all160 final composites; an independent review also verified all40 late-male composites against the original clothes and props. All160 exact original bounds, binary alpha and RGBA8 formats pass the asset suite. Provenance, prompts and SHA-256 hashes are recorded in human-expressions-20260916-manifest.json.

## Contact sheets

Uses tools/expression-contact-sheet.cjs: original PNG, production accent SVG/CSS,104px logical canvas and original cast-floor shift, uniformly2x. Static composite, not device screenshot. Viewport/background/motion omitted; sweat a labeled approximation. User receives16 stage sheets and four40-state overviews. Final iPhone acceptance remains with user.

## Final visual adjustments and scope

Human silver marks moved6 logical pixels up from the initial trial to clear hair/hats; clouds moved3 right and4 up. Zzz clearance changed from [9,-2] to [2,2] so the full stroke fits within the104-unit frame. Head-height anchors cover all16 stages. Original normal assets, all cat/dog PNGs and full SVG outputs, shared colors/outlines/reactions, game values and saves remain unchanged. Toy train, stuffed rabbit, bags, cane and the elder woman’s neutral lap cat were visually checked.

Independent code review: no actionable findings;109 connection/preview tests pass. Follow-up review confirmed all160 cat/dog paths and full SVG strings remain byte-for-byte equal to baseline. Full suite completed in129.9 seconds. Draft PR278 remains the delivery branch; no main merge.
