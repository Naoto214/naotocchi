# 季節・収穫タワーの既存イラスト利用 — 2026-09-10

## 再開時のGitHubと公開状態

PR #228は2026-09-10 13:59:52 UTCにマージ済み。
最新main `c36215e110dff32d4d36556ef73cf534b42673a7`、tree
`7cd6824ba91df36acaec19a25c63d69302819ca0` は保存HEAD `17ebb008` の内容と一致する。
[main Runtime #303](https://github.com/Naoto214/naotocchi/actions/runs/34486163019)と
[Pages #226](https://github.com/Naoto214/naotocchi/actions/runs/34486161666)はSUCCESS。
したがって、旧記録の「#228未マージ・未公開」は保存時点の履歴となった。

配信された `ui-illustrations.css` と `season-region-atlas-v1.png` はmainとバイト一致。
公開HTML・script.js・care-atlas-v2.pngは取得と再試行がタイムアウトしたため、
**Pagesのデプロイ成功と、全対象ファイルの配信一致は区別する**。
Pages成果物の参照は取得できたが、ファイル取得はHTTP 403で、成果物のバイト比較は未実施。
この時点で透過修正後の実ゲーム画面を確認できた、とは扱わない。

他open PRは旧#92のみ。Claude公開ブランチは `cc7aaa462bca784a18faf91bdade7f7447bae098` のまま。
未公開・未コミットの他タブ素材は未確認。地域選定／装飾参照の別作業は取り込んでいない。

## 今回の変更

基準mainから `feature/seasonal-minigame-art-20260910` を作成。
コード保存地点 `3bd80fcc053f3cfb51f1de973cb4f1a71874780c`、tree
`911b4af5574fdf5b98f0f7b76dd4b072c0aaa7ee`。
文書を含む最終HEAD・tree・CI・統合状態はPR本文／最新コメントを参照する。

| ゲーム | 使う既存の絵 | 失敗時 | QAセーブ |
| --- | --- | --- | --- |
| しゅうかくタワー `stack-harvest` | 麦 `wheat` | 🌾 | `stack_harvest` |
| さくらタワー `stack-sakura` | 桜 `cherry_blossom` | 🌸 | `stack_sakura` |
| おちばタワー `stack-leaves` | 紅葉 `maple_leaf` | 🍁 | `stack_leaves` |

既存 `season-region-atlas-v1` の同じ絵をCanvasの積み木の中央へ表示する。
元の15pxの絵柄枠・縦横比・透明余白を保持し、JSONのclipBoundsで隣の絵を除外。
積み木の形・幅・陰影・クレーン・背景などのCanvasは保持する。

本体がすでに読み込んでいる非表示のscenery画像を渡すため、新画像・新しい画像要求・
ロード完了待ち・画像用タイマーは追加しない。未読込／失敗／寸法違い／描画例外では
元の絵文字を出す。読込完了時は次の通常フレームから絵に切り替わる。
Canvasの描画状態を復元し、ゲーム終了後の画像イベントが再描画や報酬を起こさない。

## 検証

基準mainの145件から、7件を追加した `npm test` は **152成功・失敗0・skip 0**。
追加前の検証では未実装の描画／復旧を指摘する5件が失敗し、既存動作を守る2件は成功。
実装後は7件すべて成功。起動329 DOM ID・100ゲーム・91コレクションと既存の会話、
保存、デート／記念日、キャスト、危険通知、地域・天候のゲートも成功。

- 3素材の描画元・描画先をatlas JSONと照合。元の小枠内に全体を配置することを確認。
- 読込中、失敗、異なる画像寸法、drawImage例外、途中復旧、既存QAによる画像失敗を検証。
- 素材のない木の実・雪玉・パンケーキを別の物の絵に置き換えないことを確認。
- 実際の落下操作と着地・終了まで動かし、絵あり／代替表示でその他のCanvas命令、
  最終状態、得点・報酬・タイミングが一致することを確認。
- 動きを減らす設定・軽量モードと、終了後の画像イベントを検証。

311既存画像を全件バイト比較し、変更0・追加0。
新しい絵柄ヘルパーと描画切替だけを取り除くと、games.js全体が基準mainとバイト一致。
画像参照の受渡し3か所だけを取り除くと、script.js全体も一致する。
WORLD_MASTER、CSS各種、キャスト配置・動作、状態通知、世界設定の主要10ファイルも一致。
死亡条件・お世話・保存形式・安定ID・ゲーム抽選・会話・地域参照は保持した。

独立レビューはコード保存地点 `3bd80fc` の9ファイルを点検し、未解決Critical／Important／Minor各0。
画像差分0も再確認し、追加7テストとQAセーブの検証を独立して再実行・成功。実画面の合格判定は含まない。

[検証JSON](seasonal-stack-illustrations-2026-09-10.json)／
[テスト全出力](seasonal-stack-illustrations-test-output-2026-09-10.txt)。

## 実画面の限界と再現方法

公式ブラウザーは初期化には成功したがタブ一覧が20秒タイムアウト。
案内された復旧手順を読み、同じブラウザーで新しいタブを試しても同じタイムアウト。
**今回のゲーム画面取得は0件**。Canvas命令の記録と代替DOM／時計で行うNode検証は、
ブラウザー描画・iPhoneの操作・音・FPS・がたつき改善の証拠にはしない。

開発用 `/__qa` の上記3セーブを選択し、320／390／768pxで読み込む。
ゲーム内の「プロフィール → ゲームを選ぶ」から対応するタワーを選ぶ。
画像失敗のチェックを付けると、同じscenery画像に失敗を発生させられる。
QAセーブの再読込と対象ゲームが一覧に出ることはNodeで確認済み。
`Measure layout` はホームの配置測定であり、Canvasの絵の合格判定ではない。

従来の320px／文字拡大／26仲間＋恋人／配色／通知の出入り／画像失敗／静止・軽量設定の
実画面確認も継続する。デートと記念日の既存のユーザー判断を取り消して再確認を求めない。

残る小物は食材、木の実、道路・宇宙、カード／牌など。
おにぎりを茶碗ご飯、食材の卵を孵化用の卵で代用しない。
地域のvisualBaseIdによる装飾参照、未到達7PNG、旧#92の整理は別枠。
全体開発は未完了。
