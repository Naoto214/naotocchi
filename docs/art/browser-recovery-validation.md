# ブラウザー再接続の結果とBQ記録の訂正 — BR・BS・BT（2026-09-09）

**新しい実画面完走は0件。製品の変更はなく、全体開発は未完了。**

最新の再接続試行は末尾のBT。以下のBR・BSの結果と、BQの失敗箇所の訂正は履歴として保持する。

GitHubのPR本文・現在HEAD・直近3コミット・当該HEADのActions・最新mainを取得した。
HEADはBQ `b5a2b29b444d1763e0e99bb9a3ea2790e9cfd487`、treeは `77102c2dd98c7f949fbfd7ab30b0c03a0c9c22be`。
[Actions #239](https://github.com/Naoto214/naotocchi/actions/runs/34370152470) はSUCCESSで、後続変更はなかった。
mainは `da5c631a911504e78c8b88fae373f30b85f26b31`。PR #183はopen / Draft・未マージ・競合なし。

## 前回の失敗箇所を訂正

BQで実行した操作は、タブを作成して `qaTab` に代入した後、そのタブのダイアログを取得する一つの呼び出しだった。
全体から返ったタイムアウトを、タブ作成自体の失敗と解釈してしまった。
今回、残っていた変数を確認すると **qaTabはオブジェクト、IDは10** だった。したがってタブの参照は作成済みだった。

BQの `docs/art/qa-bq/browser-connection-limitation.json` と説明文にある「タブ参照を取得できず、getJsDialogに到達していない」という断定を訂正する。
正しくは「タブ参照は作成されたが、確認ダイアログの状態を取得できなかった」。元のタイムアウト自体は事実であり、取り消さない。
旧記録はBQの履歴として残し、このBRを失敗箇所の訂正記録とする。残存confirmの有無・内容・解除は引き続き未確認。

## 今回の復旧試行

タブ一覧は再び20秒でタイムアウトした。残っていたタブ参照からダイアログ状態を取得する操作も、同じタイムアウトだった。

検証プレビューの初回起動は `vite: not found` で失敗した。依存パッケージを以前の作業ディレクトリへ向けるリンクでは、プレビューの制限されたファイル環境から利用できなかった。
両ディレクトリのlockfileが同一であることを確認し、既にインストール済みの依存パッケージを現在の作業ディレクトリ内へコピーした。2回目の起動は成功。
package.json・lockfile・Vite設定・製品コードは変更していない。

起動成功後、残っていたタブ参照でプレビューへ移動し、その後DOMを取得する呼び出しを行った。
この呼び出し全体が25秒でタイムアウトし、実行環境から `kernel reset` が通知された。途中の結果が返っていないため、移動とDOM取得のどちらで止まったかは断定しない。
ブラウザーから画面・DOM・字幕・ごほうび消費の新しい証拠は得られなかった。プレビューは停止し、停止状態も確認した。

[各操作の結果と訂正内容](qa-br/browser-recovery-results.json) を保存した。
ブラウザー制御の別経路やゲーム内confirmの差し替えによる回避は行っていない。ブラウザー復旧の制約を、製品の不具合と断定しない。

## 保存範囲と次の再開

BRの変更はこの説明文、操作記録、チェックポイント追記のみ。BQの製品コード・テスト・再開用セーブと、承認済み301画像を保持する。
新しいテストを増やしたり、以前の自動回帰を繰り返したりして、実画面確認が進んだとは扱わない。
保存後のHEAD・tree・ActionsをGitHubから取り直し、PR本文に記録する。PR #183はDraft・未マージを維持する。

次回はGitHubの最新状態とブラウザー接続を確認する。ブラウザーの実行環境は今回のタイムアウト時にリセットされたため、古い変数の継続利用は前提にしない。
タブ作成・移動・ダイアログ取得・画面取得は、失敗箇所が分かるようそれぞれの結果を記録する。
接続が利用可能になったら、BQの開発用セーブを使って特別/深海デート全編、BP-1の1周年/金婚式、10周年の終了後復帰、残り9伝説物語の順に進める。
物理iPhone/Safari・Android、全248形態の個別動作、全恋人×全プラン×全台詞分岐、100ミニゲームの手動完走も未確認のまま。

## BS：タブ作成・移動と画面取得を分けて再確認

GitHubのPR本文・現在HEAD・直近履歴・当該HEADのActions・最新mainを再取得した。
BR `a92ab771eefc64d88cb32f9975764ad4f6aaed28` / tree `d2fe437bcf8e4d4b46aba13f464a2113131b960c` / [Actions #240 SUCCESS](https://github.com/Naoto214/naotocchi/actions/runs/34371055177) から後続変更はなかった。
mainは `da5c631a911504e78c8b88fae373f30b85f26b31`、PR #183はopen / Draft・未マージ・競合なし。BRから継続し、BP/BOへ巻き戻していない。

前回の実行環境リセット後、正規のブラウザー接続を初期化し、同じ選択済みChromeで操作した。
現在のcheckoutの開発プレビューは初回起動でrunningになった。今回は結果を操作ごとに返して、複合呼び出しの失敗を推測で分解しないようにした。

| 操作 | 実際に返った結果 |
| --- | --- |
| タブ一覧 | `CDP operation refresh tabs timed out after 20000ms` |
| 新しい検証タブの作成 | 成功、ID `11` |
| このタブをプレビューのトップへ移動 | `Navigation completed` を取得 |
| DOM snapshot | `CDP operation refresh tabs was superseded by browser recovery` |
| 上記の復旧通知後、タブ一覧を1回再取得 | 同じ `superseded by browser recovery` |
| 別の対応済みAPIで表示DOMを取得 | 同じ `superseded by browser recovery` |
| 画面撮影 | `CDP operation refresh tabs timed out after 20000ms` |
| ID 11の確認ダイアログ取得 | 同じ20秒のタイムアウト |
| ブラウザー一覧 | 選択済みChromeが引き続き検出された |

**タブの作成とページへの移動は、それぞれ単独の成功応答を得た。画面取得は成功していない。**
移動完了の応答だけでは、ゲームの描画・画像ロード・操作可能性を確認したことにはならない。
特別デートのボタン操作にも進めておらず、新しい全編観測・字幕・写真・ごほうび消費・復帰の証拠は0件。
ID 11へのダイアログ問い合わせも失敗したため、元のタブに残っている可能性があるconfirmの有無・内容・解除については何も結論できない。
今回の接続エラーを製品のconfirmの不具合とは断定せず、差し替えや別経路のブラウザー制御は行っていない。

プレビューのrunning状態を確認した後、停止し、最終statusでもstoppedを確認した。
[BSの操作記録](qa-bs/browser-recovery-results.json) を保存する。変更は記録3ファイルのみで、ゲーム本体・承認済み画像・テスト・検証セーブは未変更。
新しい自動テストの追加・既存回帰のローカル再実行は行っていない。保存したHEADのActionsはGitHubから再取得し、PR本文へ記録する。

次に必要なのは、ブラウザーから実画面を取得できる状態への復旧である。接続が使えるようになったら既存BQの検証セーブで、特別/深海デート全編、BP-1の1周年/金婚式、10周年の終了後復帰、残り9伝説物語へ進む。
その他の未確認範囲もBRから変わらず、全体開発は未完了。PRはDraft・未マージを維持する。

## BT：実ゲームと選択画面は復旧、プラン選択で再び応答停止

GitHubのPR本文・現在HEAD・直近8コミット・当該HEADのActions・最新main・再帰ツリーを取得した。
HEADはBS `3161713500b2b6f61277d90dc0b622916f504dde`、treeは `02ff17f25affba84d67ccdd839399ac54bb2736b`。
[Actions #241](https://github.com/Naoto214/naotocchi/actions/runs/34372248595) はSUCCESS。
mainは `da5c631a911504e78c8b88fae373f30b85f26b31`、PR #183はopen / Draft・未マージ・競合なし。
全466ファイルの実内容から算出したGit blob SHA・modeがGitHubと一致することを確認し、このHEADの独立した作業コピーで継続した。巻き戻しはない。

今回はタブ一覧、空タブのDOM取得、既存 `/__qa` への移動、QAページのDOM取得が成功した。
幅320×高さ640pxで `special_date` を「Load and observe next movie」から読み込み、ゲーム本体のDOMを取得できた。
25歳・そだち85・所持金123,456,789・ロボットの配偶者・仲間26体の名前を確認した。画像の復号・見切れは写真や計測で確認しておらず、DOM取得を画像品質検証には数えない。

最初は誤って「きゅうあい」を操作し、求愛の台詞が出た。その後に病気の通知も出たが、デートは開始していない。
この探索を成功・製品不具合に含めず、検証セーブを読み直して「せかい」→「デートに さそう」へ進んだ。
選択画面のDOMで「ぶらぶら みてまわる」「ひなたぼっこ」「どうでも いい はなしを する」の3候補を確認した。

| 操作 | 実際に返った結果 |
| --- | --- |
| 「ぶらぶら みてまわる」を選択 | `js execution timed out; kernel reset, rerun your request` |
| リセット通知後のブラウザー初期設定 | 成功 |
| 公式のブラウザー復旧手順を確認 | 成功 |
| タブ一覧を再取得 | `CDP operation refresh tabs timed out after 20000ms` |
| ID 1の参照再取得とダイアログ取得を含む呼び出し | 全体が25秒でタイムアウト、再びkernel reset通知 |

選択クリックには1,500ms、実行全体には10,000msを指定したが、ツールの報告実時間は約21.4秒だった。
ソース上ではこの選択から `window.confirm` を呼ぶ。しかし、**確認ダイアログが実際に表示されたか、内容、承諾・取消・ごほうび消費は未確認**。
最後の参照再取得とダイアログ取得は途中の結果を返していないため、どちらで停止したかを断定しない。
ゲーム画面が読めた後にプラン選択で接続が停止した事実を記録し、製品confirmの不具合という結論にはしない。
別経路のブラウザー制御、製品confirm・時計・乱数の差し替えは行っていない。

**新しい実画面全編・字幕列・写真・消費/復帰の確認は0件。深海デートには進めていない。**
観測ボタンを押した事実だけでは連続観測の完了と扱わず、結果JSONを回収できなかった試行にフレーム数やPASSを付けない。
プレビューを停止し、最終statusでもstoppedを確認した。
[BTの操作記録](qa-bt/browser-recovery-results.json) に成功した段階・失敗・未確認を保存する。

初回のnpm testは途中の標準出力を得たが、続きの取得が `network approval was cancelled before a decision was returned` で失敗し、完了とは扱わなかった。
その後、既存QAルートテスト単独のexit 0、および `npm test` 全体のexit 0を取得した（DOM287 / ミニゲーム100 / variant collections91）。
テスト・検証セーブの追加変更はなく、Nodeの成功を実画面確認の代用にしていない。BTの保存変更は記録3ファイルのみ。
製品コード、WORLD_MASTER、承認済み301画像、旧キャスト/旧PERFECT/作者④⑤互換はBSから保持する。

次回はGitHubの最新HEADを再取得し、まずブラウザーのタブ・ネイティブダイアログを読める状態か確認する。
接続が利用できれば既存の特別デート・深海デートの全編を検証する。通常の画面操作だけ復旧した場合は、ごほうびなしの `deepsea_date` を通常デートとして検証できるが、特別デートの成功には数えない。
BP-1の1周年/金婚式、10周年の終了後復帰、残り9伝説物語、物理端末、全248形態動作、全恋人×全プラン×全台詞分岐、100ミニゲーム手動完走も未確認のまま。
全体開発は未完了。保存後のHEAD・tree・当該HEADのActionsを再取得し、PR本文へ記録する。明示的なマージ依頼までDraft・未マージを維持する。
