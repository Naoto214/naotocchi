# ムービー全編の継続確認 — BP（2026-09-09）

PR #183 / `feature/character-cast-art-v1` をGitHubの実状態から再開した。
開始HEADはBO `d89aee56e54464b4904e4f13b30c527fd88faa91`、treeは `64d02673d753a62a0be993c75524ae4f1ad9ef40`。
[Runtime smoke / dialogue regression #237 SUCCESS](https://github.com/Naoto214/naotocchi/actions/runs/34362760466)、PR本文・直近コミット・mainを取得し、BO以後の変更がないことを確認した。
mainは `da5c631a911504e78c8b88fae373f30b85f26b31`（PR #203、ミニゲーム100本）。開始時の全426ファイルをGitの実体と照合した。巻き戻しは行っていない。

## 変更した表示と検証機能

BO-2の `scrollIntoView({ block: 'nearest' })` 後にも、10周年のムービーパネル上端が **-0.078125px** に残る実測を得た。
`.date-movie` に `scroll-margin-block: 8px` を追加し、スクロール先に余白を設けた。
CSS読込版は `20260909-cast-layout-bp-1`。製品の変更はこの余白とCSSの読込版のみで、`script.js` はBO-2の実バイトを保持する。
伝説の抽選率・台詞・報酬・ゲーム時計・セーブ形式には変更を加えていない。

開発専用 `/__qa` に、場面のロード時から次のムービーを待つ観測を追加した。
iframeのDOMが利用可能になり次第観測を始め、画像ロード完了を待たず、通常のtick・乱数・操作で起きたムービーを記録する。
字幕の順序と時刻、開始直前から最初の表示までのフレーム間隔、観測中の最大間隔、終了ボタン出現時刻を保存する。
終了ボタンの出現後も1秒観測する。開始前の非表示、連続サンプリング、期待した画像・全字幕・終了ボタンを併せて確認し、汎用の `checksPass` だけでは完走と判定しない。

10周年のスクロール検証には開始まで約12秒あるセーブを追加した。3秒後に開始する従来のセーブでは、検証操作のスクロールがムービー開始と重なることがあったためである。
伝説には、既存の消耗品効果である病気予防12回を保持したセーブを追加。未遭遇の伝説をダイオウイカだけにし、通常の抽選を待った。ゲームの時刻・タイマー・乱数・クロージャーの差し替えは行っていない。

## 最終BP-1の実画面で全編を確認した範囲

このcheckoutのHTML/CSS/JS・承認済み画像を、Sitesの開発プレビューとCloud Browserで実行した。
公開mainや以前のプレビューの記録は流用していない。指定幅320×高さ640px、実clientWidth305px、仲間26体。

| 場面 | 全字幕 | 観測フレーム | 終了ボタン表示フレーム | 最大フレーム間隔 | 配置失敗・読込中・破損 |
| --- | --- | --- | --- | --- | --- |
| [伝説・ダイオウイカの別物語](qa-bp/legend-final.jpg) | 7 | 1,516 | 60 | 46.9ms | すべて0 |
| [開始前にお世話側へスクロールした10周年](qa-bp/anniversary-10-final.jpg) | 5 | 1,288 | 62 | 37.8ms | すべて0 |
| [銀婚式](qa-bp/anniversary-25-final.jpg) | 6 | 1,537 | 61 | 46.0ms | すべて0 |

計 **4,341フレーム・18字幕**。全試行で最初の字幕から最後の字幕と終了ボタンを確認した。
伝説・銀婚式は終了操作後の育成画面への復帰も計測した。10周年は終了ボタンをクリックしたが、復帰状態の独立した記録を残せていないため、復帰確認済みの件数には含めない。
主人公のアニメーションは `date-actor-walk`。PNGを表示するキャラ枠の場面枠外へのはみ出し、字幕との重なり、字幕のoverflow、キャラ行全体の横overflow、終了ボタンを含むパネル全体の画面外、ページの横overflowを計測した。直接テキストで表示するダイオウイカemojiは個別の枠計測の対象に入らず、写真で目視確認した。
読込中・破損の検査はムービー画像だけでなく、背後を含むゲーム内の寸法を持つ画像を対象としている。

伝説は開始前に「じゃれる」を操作してお世話側へスクロールし、通常抽選で約144秒後に発生した。man/06.pngを表示し、7字幕の全文・順序を実装の当該物語と照合した。
最初の可視フレームへの間隔は19.8ms、観測時間は約26.03秒。終了後は所持金374の育成画面に復帰した。
ダイオウイカ自体は既存のemoji表示を保持。これは伝説5種×各2物語のうち **ダイオウイカの1物語** の実画面確認である。

10周年は開始前に「ごはん」を操作し、約12秒後の自然発生から全編を観測した。主人公man/06.pngとロボットの専用PNGを表示した。
銀婚式は50歳のman/07.pngとロボットのPNG、6字幕を確認した。
スクリーンショットは各瞬間の写真であり、全字幕の動画ではない。全文と時刻は [実測JSON](movie-completion-browser-results.json) と `qa-bp/*-final.json` に保存している。

## 修正前・探索中の記録を分けて保持

| 試行 | CSS | 結果と扱い |
| --- | --- | --- |
| 伝説の最初の待機 | BO-2 | 発生する前に寿命を迎えた。全編確認に含めない |
| 伝説の次の試行 | BO-2 | 7字幕・1,515フレーム、うち1,353フレームがパネル画面外。失敗として保持。待機中のお世話操作と開始の重なりを除外できず、この値の全原因を丸め誤差と断定しない |
| 1周年 | BO-2 | 5字幕・1,272フレーム、配置失敗・読込中・破損0。BPのCSS修正前の成功 |
| スクロール済み・所持金123,456,789の金婚式 | BO-2 | 6字幕・1,530フレーム、各失敗0。上端3.921875px。BPのCSS修正前の成功 |
| 10周年・開始に近い時点のスクロール | BO-2 | 1,270フレーム中926が配置失敗。最初の失敗は上端-5.078125px、後の単独計測では-0.078125px |
| 10周年・開始とスクロール操作が重なった再試行 | BP-1 | 1,275フレーム中458が配置失敗。最初の上端-135.078125px。操作条件が異なり、成功には含めない |
| 12秒後に開始する10周年の先行試行 | BP-1 | 1,281フレーム中、配置失敗0・読込中4・破損0。4フレームは背後を含む28画像の読込中判定。開始前スクロールを十分に確定できず、最終成功には含めない |

これらの生データと写真を上書きせず保存した。前回BO-1の記念日4種類、BO-2の金婚式・通常デート・伝説の終了場面とも区別する。
BP-1の最終10周年と伝説では、ムービーが始まる前にお世話ボタンの操作を済ませ、表示中はスクロール操作を加えていない。

## 自動回帰と保存確認

実際の伝説スケジューラーをNodeの既存ハーネスに接続し、次を追加検証した。

- そだち不足・遭遇済み・自由モード・卵・睡眠・変身選択中の6状態で発生しない。
- 図鑑・実績・デザイン・プロフィール・通信・アイテム・せかいの7メニュー中は発生せず、年齢も進まない。
- 当選しない乱数では発生せず、5伝説それぞれで未遭遇のものを選び、報酬を受け取る。
- ムービー中は年齢が止まる。実際のスキップボタンのハンドラーが終了ボタンを表示し、スキップを隠し、閉じる前に35秒進めても予約字幕が再開しない。
- 閉じた後に再度判定しても同じ人生で再生・報酬取得を繰り返さない。

スキップのハンドラー処理を読み込み時に無効化した一時的な変異検証では、`Skip did not expose Close` で失敗した。製品ソースはその検証のために変更していない。
これらは制御した時計・乱数による自動回帰であり、実ブラウザーの確認件数には加えない。

最終 `npm test` は成功（DOM287 / ミニゲーム100 / variant collections91）。既存の180デート・72記念日・伝説10物語、旧コアラ・旧きのこ・時計・カタツムリ・育成キノコ、作者④⑤・旧PERFECTの作者・王冠・自由モードの回帰も成功。
開発QAのセーブ生成・埋込scriptの構文確認、`git diff --check` が成功。独立した読み取り専用レビューで重大な指摘は残っていない。
承認済み301画像（キャラPNG294＋ゴールJPEG7）はBO記録のSHA-256・Git blob SHAに全件一致。WORLD_MASTERと製品JSもBOからバイト不変。
ソース・証拠ファイルのハッシュは [検証JSON](movie-completion-validation.json) に保存する。
保存後の確定HEAD・tree・当該HEADのActionsはPR #183本文とChecksに記録する。

## 次に残る確認

特別デートは、ロボット・ごほうび1個・所持金123,456,789で「せかい」→「デートにさそう」→提示された「あめやどり」まで進めた。
選択操作が `Input.dispatchMouseEvent timed out` となり、その後の確認ダイアログ取得と画面撮影も `CDP operation refresh tabs timed out after 20000ms` で失敗した。
特別デートの全編・ごほうび消費は今回の実画面確認に含めない。ゲーム内confirmの実装を変更して回避していない。深海デートはこの後に予定していたが、今回は未実施。

- 物理iPhone/Safari・Android。
- 全248形態の個別動作、睡眠・病気・装備などとの全組み合わせ。
- 伝説の残り9物語の実画面全編、すべての開始条件・分岐の実画面。
- BP-1での1周年・金婚式の再実行（今回の両成功はBO-2のCSS）。
- 全恋人×全プラン×全記念日×全台詞分岐の実画面、特別デート・深海条件の全編。
- ミニゲーム100本の手動完走。

全体開発は未完了。BL・BM・BN・BOの既存記録を保持し、確認済みの範囲から継続する。
PR #183はopen / Draft・未マージを維持し、ユーザーの明示的な依頼までマージしない。
