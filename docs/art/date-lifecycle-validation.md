# デートの消費・保存・復帰の継続検証 — BQ（2026-09-09）

PR #183 / `feature/character-cast-art-v1` の継続記録。**全体開発は未完了。今回の新しい実画面完走は0件。**

## 再開時のGitHub実状態

PR本文、現在HEAD、直近5コミット、当該HEADのActions、最新main、再帰ツリーをGitHubから取得した。
HEADはBP `7a4f7d8db5c6885f93df99587845072370a59fa4`、treeは `f0dbe178416c1e9479a8a0aee092c014cc6e5b31`。
[Runtime smoke / dialogue regression #238](https://github.com/Naoto214/naotocchi/actions/runs/34368234028) はSUCCESS。
BP以後の変更はなく、全457ファイルのblob SHA・modeを手元のコードと照合してから、別の作業ディレクトリへコピーした。巻き戻していない。
mainは `da5c631a911504e78c8b88fae373f30b85f26b31`（PR #203）、ミニゲーム100本。PRはopen / Draft・未マージ・競合なしだった。

## 今回追加した自動検証

既存のNodeハーネスでゲーム本体のクロージャーを実行する。DOM・時計・乱数・confirm応答・保存先は検証用であり、ブラウザーの描画・ネイティブconfirmの操作結果ではない。
消費判定・台詞生成・保存・読込・選択/スキップ/終了/キャンセルのハンドラー・ゲームのloopは製品コードを使用する。

| 選択画面から進めたケース | ごほうび開始→終了 | 確認応答 | 確認した終わり方 |
| --- | --- | --- | --- |
| ロボット・特別デート | 1→0 | 承諾 | 全7字幕・終了・育成へ復帰 |
| ロボット・特別デート | 2→1 | 承諾 | 8秒後に実スキップ操作・予約字幕停止・終了・復帰 |
| ロボット・通常デート | 1→1 | 辞退 | 全4字幕・終了・復帰 |
| ロボット・通常デート | 0→0 | 確認を呼ばない | 全4字幕・終了・復帰 |
| 深海のアンコウ・リング所持・特別デート | 1→0 | 承諾 | 全7字幕・リングの一言・終了・復帰 |
| 深海のアンコウ・通常デート | 0→0 | 確認を呼ばない | 全4字幕・終了・復帰 |

6ケースで、専用PNG参照、通常/特別の見た目用クラス、デート回数、所持金123,456,789の保持、選択/再生中の年齢・クールダウン停止を確認した。
実際の保存出力を `loadState()` に渡し、消費後の個数と特別な思い出・デート回数・相手が保持されることを検証した。
終了後に35秒進めても予約字幕が再開せず、loopで年齢が1tick進み、クールダウンが60から59へ進む。直後の再勧誘では再デート・再消費・再確認をしない。
全編ケースは字幕切替直前/直後と終了ボタンの出現時刻を確認した。スキップケースを全編完走に含めない。

別に、選択画面のキャンセルが状態・ごほうびを変えず、画面を閉じ、育成の時計を再開することも確認した。
深海の全10プラン×3種類の冒頭本文＝30ケースでは、各プランで異なる3本文を確認し、全4字幕と終了・復帰まで検証した。
深海用の5プランに陸上用の冒頭表現が漏れず、光る魚・岩かげ・海の光の思い出が地域に合うことを確認した。
これはアンコウを相手にした限定条件で、全恋人・全締め台詞・全乱数組み合わせの網羅ではない。

### テストが故障を検出することの確認

製品ファイルを書き換えず、Nodeへの読み込み時だけ4つの故障を個別に入れた。
キャンセルを無操作にする、特別デート消費後の保存を抜く、特別デートの終了を再生終了だけにする、深海の思い出を陸上へ戻す、の4件すべてで期待する検証が失敗した。
[変異と実際の失敗メッセージ](qa-bq/date-mutation-results.json) を保存した。
レビューでキャンセル後の画面・時計検証の不足を指摘されて補強した。初回作成時の期待クールダウン8と実値60の不一致も、既存仕様を読み直してテスト側を訂正した。製品の不具合や修正前の再現とは扱わない。

## ブラウザーの制約と実画面の未確認

ブラウザー接続の初期設定は成功したが、タブ一覧取得が `CDP operation refresh tabs timed out after 20000ms` で失敗した。
案内された復旧手順を確認し、既存Chromeの検出はできたものの、新しいタブの作成も同じエラーで失敗した。
タブの参照を取得できず、今回の `getJsDialog` は実行段階に到達していない。別のタブ一覧は空だったが、残存confirmがない証拠にはならない。
[操作結果](qa-bq/browser-connection-limitation.json) を保存した。

そのため開発プレビューは起動せず、新しい写真・全編観測・ごほうび消費の実画面確認は**0件**。
ネイティブconfirmの状態は未解決で、前回の停止を製品バグと断定しない。製品のconfirm処理を変更せず、別のブラウザー操作手段やゲーム内関数の差し替えで回避していない。

BPの全編成功3件・18字幕・4,341フレームはそのまま保存する。BO-2の1周年/金婚式や失敗記録をBP-1の成功へ繰り入れず、今回のNode結果も実画面件数へ加えない。

## 再開用の検証セーブ

開発専用 `/__qa` に9セーブを追加した。ゲームの時計・乱数・クロージャーを変更する仕組みは追加していない。

| セーブ | 用途 |
| --- | --- |
| `anniversary_1_scrolled` / `anniversary_50_scrolled` | 開始まで4tick（通常約12秒）。50周年は所持金123,456,789 |
| `legend_gate_cared` / `legend_stairs_cared` / `legend_lamp_cared` / `legend_mirror_cared` | 指定の伝説だけ未遭遇。既存の病気予防効果12回を保持 |
| `special_date_two` | ごほうび2個からの1個消費とスキップ |
| `special_date_ring` | リング所持の特別デート |
| `deepsea_special_date` | アンコウ・深海・ごほうび1個 |

既存の `special_date`、`deepsea_date`、`anniversary_10_scrolled`、`legend_boss_cared` も保持する。
伝説セーブは遭遇済み履歴で対象を絞るだけで、発生時刻や2物語の選択を強制しない。残り9物語の完走準備はこの制約のもとで行う。
実際に生成した開発ルートの埋込scriptとセーブを検証する `tests/visual-qa-test.cjs` を `npm test` とActionsに追加した。

## 保持した内容と次の作業

製品HTML/CSS/JS・WORLD_MASTER・画像仕様はBPからバイト不変。CSSはBP-1、JSはBO-2のまま。
BPのハッシュ一覧と照合し、PNG294枚＋ゴールJPEG7枚＝301枚すべてが一致した。
育成31種248形態、育成キノコ・カタツムリ・時計、旧コアラ/旧きのこ、作者④⑤、旧PERFECTの作者・王冠・自由モード、100ミニゲームを保持する。
[テストログ](qa-bq/npm-test.log) と [検証・ハッシュ記録](date-lifecycle-validation.json) を参照。

次回はGitHubの最新HEADを確認したうえで、まずブラウザーのタブ/ダイアログ状態を再確認する。接続できたらこのブランチの既存Vite設定と `/__qa` を使ってプレビューを起動する。
優先順は特別デート・深海デートの全編、BP-1の1周年/金婚式、10周年の終了後復帰、残りの伝説物語とキャラ動作。
終了ボタンを押しただけで復帰確認済みにせず、その後のレイアウト計測を独立して保存する。confirm待機等で観測が途切れた場合は、その間隔と限界も記録する。
物理iPhone/Safari・Android、全248形態の個別動作、全恋人×全プラン×全台詞分岐、100ミニゲームの手動完走も引き続き未確認。

確定HEAD・tree・そのHEADのActionsは保存後にGitHubから再取得し、PR本文とChecksへ記録する。PR #183はDraft・未マージを維持する。
