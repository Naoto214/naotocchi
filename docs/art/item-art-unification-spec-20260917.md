# Item art unification — approved scope

User-approved 2026-09-17: finish all 27 existing item images as one art set. The audit's 16/7/4 groups indicate revision effort, not exclusion. Characters, partners and companions are the highest visual reference. Preserve recognizable motifs, not inconsistent rendering. Fix shop 26px vs 40px frames and transparent-margin differences. Audit all-item sheet, actual shop, pet with each equipment, and pet+partner+companions+equipment; iterate on visible outliers. Do not merge or publish.

Current main 61005d1ff6535113fd5ffbb98b615252d1a7b323. Scope: 10 equipment, 4 achievement items, 12 consumables and normal sticker pack. The themed pack is a text purchase control with no existing dedicated art.

All 27 must have explicit assets/manifest entries and item-specific render paths, consistent transparent 128px masters, controlled 104–112px long-side visible extent, soft fine pixel contours and restrained shading. Retain existing 16 motifs/art where sound; extract/reframe to shared masters instead of globally changing UI atlases. Parent supplies four imagegen redraws (bento/toy/first-aid/game-pass). Seven light revisions comprise time-back/time-forward, consumable life charm, match/transform/rare-friend tickets and rare egg. Existing native SVG symbols may be edited and rasterized; reuse the existing face-free egg for rare egg with a distinguishing small symbol. Avoid new faces on objects. Keep normal egg unchanged in motif.

Equipment and goal IDs retain game behavior. Item catalog names/effects/prices/save schema are unchanged. Shared UI symbols and character assets remain unchanged. 40px shop image frames; retain existing home frame/layout (roughly 31px) and ring-between-pet-and-partner behavior. All image failure fallbacks remain functional. Use semantic item IDs, not glyph-wide replacement, so back/forward and normal/rare stay distinct. Keep distinct display identity for sticker pack and generic gift UI.

Implementation audit: native SVG revisions were visibly too flat in the all-item comparison. The controller supplied four additional imagegen sources (time-back/time-forward, transform and match), bringing redraw sources to eight. Three light revisions reuse retained art plus small identification markers. The other sixteen remain reframed retained art. This is a rendering-method change in service of the approved all-27 scope, not a change to motifs or game behavior.
