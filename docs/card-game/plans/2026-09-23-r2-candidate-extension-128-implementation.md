# 128 detailed TDD plan

1. Verify remote PR head/tree, draft/open/unmerged; check four 127 raw hashes and states. Establish clean local branch at 127.
2. RED: tests for source-grounded non-independent board classification (including missing registry and forged category), targetless world ID and rejection of forged targets, user-approved 114 variant IDs when one source/action offers several choices, mixed 02-B comparison with all four candidates and 116 safe subset, and independent four-route replay. Run each test and record failure.
3. GREEN: add a 128 adapter around the immutable 121/127 enumeration and replay. Keep the three mechanisms in separate functions and use the original 116 resolver. Add no card copy conditional; require source metadata for each board card and paid effect.
4. Regenerate only 128 canonical plan, evaluation and four new stop records; write hash-chain and source-boundary validators. Wire `--checkpoint-128` into the aggregate checker without changing legacy expected values.
5. Verify dedicated tests, canonical JSON, protected hashes, full proxy suite (separately report the 117 known failure), local tree diff, and remote HEAD/tree/status. Save on the design branch and update PR #259 while keeping it Draft/open/unmerged.
