# 274 混合選択計画

273のraw/canonicalと272保存state/hashを照合。2件は唯一passで選び、02-Aは再構成した合法response候補を116/119の既存seeded fallbackへ渡す。02-Bは確定した単一リンクのコイン解決処理を指定する。公開情報を選択へ先取りせず、RED→GREEN、canonical bytes、GitHub保存後remoteを確認する。
