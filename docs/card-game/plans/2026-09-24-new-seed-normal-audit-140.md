# 140 新seed通常行動監査の実装計画

## 目的と境界

138の停止原本は変更せず、responseを双方passした3保存stateを、125・127・128・132で確立済みの候補監査へ接続する。02-AのG-hit-blow発動前停止は保持する。140では候補監査だけを行い、戦略的選択・時の支払い・対戦eventは証明済みとして記録しない。独立balance標本は0。

## RED→GREEN

1. 138 raw SHA、135への再生鎖、保存stateのgame/continuation hash、snapshot末端を検査。改ざん・経路欠落で停止するテストを先に書く。
2. 現在stateを121のowner/public projectionへ渡す。空familyは125、盤面枚数は127、対象なしIDは128、盤上能力は132を再利用し、12条件と合法候補を監査する。過去経路用の132 `_history` を新seed履歴証明として流用せず、保存済み138のevent連鎖を入口検査する。
3. 3件の候補集合と12条件の完全性、02-Aを進めないこと、canonical bytes再生成一致、過去JSON非変更をテストする。比較と遷移は別checkpointで、既存比較器の旧経路ID依存を外してから取り扱う。
4. 専用テスト・全proxy回帰・設計データ検査・差分検査を実施し、117旧テストの期待190／実測263だけ分離する。結果を記録してDraft PRへ保存し、workflow/status件数を実測する。
