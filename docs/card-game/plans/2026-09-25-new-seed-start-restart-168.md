# 168 TDD計画：4経路開始時response初回選択

167 raw SHA、166全候補監査、165 state/hashを独立再生して、119の優先比較とseeded fallbackを4開始時機会へ適用する。盤上能力IDは167の単一source一般契約を使い、既存pass・hand quick-use IDと正準ソートする。初回passなら1 eventだけ記録して次の優先者候補を再監査する。quick-useまたは盤上能力が選択された場合は、発動前で保持する。

REDは4 decision・event/hash連鎖・改ざん拒否の専用2テスト。GREENは138/119 decisionとpass遷移を再利用する。選択されないC-chickenの山札上効果を実行せず、次の優先者やitem起動を先回りしない。4経路とも既にseeded fallbackを含むため独立balance標本0を維持する。
