# 132 active board ability and nearby contracts

Input: remote 131 HEAD cdc4da386558e9b919d04f9e198efcea02614298, tree 4d6577a34a39730827b04704b927f4308ff41004, four immutable stop raw bytes and dual state hashes. The 114 template for C-cat_friend specifies a normal-action board activation, zero cost, a different named companion in own discard as one target, and per-instance once per turn. The 72 text confirms this is optional activation that moves the source to deck bottom; no trigger, passive, or response timing. 121/128 classify continuous, past triggers and no-ability separately. Only sources explicitly classified by current text and 114 timing can become independent normal-action board units; others remain non-independent or stop if unclassified. Enumerate each eligible target; when no eligible target, emit one excluded unit with `required_target_absent`, never an invented candidate. If source and target meet the single-target contract, use 127's `candidate-{action_type}-{source_instance_id}-target-{target_instance_id}` (same grammar, board-source proof), not a new ID family. A used-this-turn source is excluded; an unverified use history stops rather than assuming unused. Current four sources were just placed; their saved placement/response event segments prove no activation since placement, and no qualifying discard companion exists.

Independent near-state audit: after this exclusion, 01 has the already classified paid G-hit-blow/W-city/birth set and pass; 02 has I-bond1/I-bowtie and I-c_coin2 among paid actions and pass. Reuse 131's hidden-top coin and 129's conditional growth evidence in a full paid-to-pass comparison; do not read deck top. If pass reaches turn end, 131 already saved the C-cat_friend placement event and the card remains on board. Extend the read-only 123/124 provenance registries in a scoped adapter with its source text: a player-activated ability is not an automatic turn-end trigger, placement has immediate growth 0 and duration none. Re-derive history, never trust a saved completeness flag.

Stop for any new board ability, target rule, usage provenance, effect, or response not fixed by existing canon. No card-copy or path-specific rule and no generic game engine; no edits to earlier checkpoints.
