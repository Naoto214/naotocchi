# 198 新seed2通常行動候補監査 TDD計画

197 raw SHAと4状態を固定。通常行動入口2件は既存の候補表、公開・所有情報投影、12検査、盤上response_triggered除外契約で再列挙する。候補IDと候補完全性を先にREDテストで固定し、改変候補拒否も検証する。終了入口2件は履歴監査待ちとして保持。eventなし、canonical JSON一致と設計データerrors空を確認しGitHubへ保存する。
