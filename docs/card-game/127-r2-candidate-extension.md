# 127 R2通常行動候補の不足証拠と4経路独立再開

126の4 stop raw SHAと両state hashを再検証し、127では121・125を変更せずに、114の盤面枚数variantの除外理由と、承認された単一対象IDを現在stateから再導出した。対象なし／複数対象のIDは追加しない。01-Bの5候補は107/114の確定そだち・時収支を比較し、116の安全な時0配置2件だけをseed抽選集合へ入れた。5件すべてを合法候補に保持する。カブトムシ①の任意の手札交換は確定そだち・時の増加ではないが、将来価値を加点してはいない。

4経路を126保存stateからそれぞれ独立再開した。01-Aでは終了申出と応答後の`return_target=turn_end`を正しく閉じ、117からの全履歴と新eventの両hashを連結して123の6手順を再証明した。R2相手ターン開始・卵交換後も通常の既存handlerを使い、各経路を次の未証明箇所で止めた。

| 経路 | 126→127 seq | 127で処理した判断 | 現在の真正停止 |
| --- | --- | --- | --- |
| order-01-a-first | 20→28 | 通常pass、終了応答、終了6手順証明、次ターンの卵交換、無料なかま配置、双方応答 | 盤面に出た能力なしなかまの独立通常行動分類が121 registryに未接続：`unresolved_canonical_predicate` |
| order-01-b-first | 20→23 | 5候補比較、2無料配置から116抽選、なかま配置、双方応答 | 同じ盤面分類不足：`unresolved_canonical_predicate` |
| order-02-a-first | 18→18 | 装備の単一対象IDは導出。候補集合にはさらに対象なしセカイ配置がある | 対象なしセカイ配置のstable ID未定義：`missing_candidate_id_grammar` |
| order-02-b-first | 18→18 | 装備の単一対象IDを導出し、合法候補集合を完全化 | 無料こいびと配置、有償birth、対象付き装備、pass間の107/114選択証明不足 |

127計画・評価と各新stopは`data/proxy-r2-candidate-*127*`に保存。集計はplanned 4／completed 0／rules-stop 4、127新decision 9／event 11／snapshot 15、winner 0／独立balance標本0。126保存raw/state/hash、112と過去の保護対象、カード本文・数値・登録区分は変更しない。117旧テストの期待190・現行263差は独立の既知問題として扱う。新HEADのGitHub workflow/statusは実測の有無で報告し、0件を成功とは扱わない。

次の設計判断は、(1) 能力なしなかまを含む盤面能力分類を既存本文に基づく一般registryへ接続する範囲、(2) 現在到達した対象なしセカイ配置ID、(3) 02-Bの複数種類の候補の比較証明。いずれも未承認の規則やIDを推測せず、127では状態を維持して止める。
