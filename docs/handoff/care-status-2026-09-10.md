# お世話イラスト・状態表示：初回実装後の引継ぎ

作業ブランチ：`feature/care-illustrations-status-20260910`。
基準main：`068864a7ddc44bc6f6e9ab4a6bb41f7e597749a2`。
依存PR #224：`0a099b6bdcc4f376004f8d9155b49c43895dbe33`。
保存前の確認では#224はDraft・未マージ。今回のブランチはその修正を親として保持する。
新PRはmainを宛先にして既存CIを実行するため、#224統合前は監査差分も含む。
マージ済み・公開済みと推測せず、再開時に両PRの実状態を読む。

## 再開手順

1. 最新main・open PR・各PR本文・HEAD・CIを再取得する。別タブの変更を正として
   差分を読む。既存作業に上書き・二重実装をしない。
2. [実装と検証](../qa/care-status-2026-09-10.md)、[JSON](../qa/care-status-2026-09-10.json)、
   [テスト出力](../qa/care-status-test-output-2026-09-10.txt)、
   [素材](../art/care-icons-v1-2026-09-10.md)、最新チェックポイントを読む。
3. このブランチのPR本文に記録した確定HEAD・tree・CIと、チェックアウトの一致を確認する。
   全体開発は未完了。勝手に両PRをまとめてマージしない。

初回の小イラスト・危険通知・他パラメーター表示は実装済み候補。
`care-status.js` が読み取り専用で状態を判定し、既存2行の通知領域を利用する。
危険が続く間は表示を消さず、原因・次のお世話・対象ボタンの印を示す。
自然減少の通知は増やさず、操作増減と成長・回復・おとろえの節目をまとめる。
元の結果説明も全文を残し、危険の背後で節目を失わないようにした。

承認済み301画像／現行307画像は変更0。248形態の顔へ一律に目口を重ねていない。
既存の卵、仲間の左右の弧、字幕、保存形式・安定ID・会話・ゲーム・死亡速度を維持する。
正しい無限判定は現行 `isImmortal()`。古いコメントのそだち・リング条件を復活させない。
素材は白背景の一枚を絵ごとの座標で表示する。透過と誤記しない。

## 次に残る作業

- 取得可能な実ブラウザーで追加した `care_*` QA sceneを確認する。320px程度、
  大きな文字、26仲間、天候、各配色、長い病名、通知とボタンの固定位置、結果の読みやすさ、
  減らす設定を中心にする。保存されたユーザーの子を危険状態にして試さない。
- iPhoneの描画・タッチ・音・FPSを別に確認する。今回も接続タイムアウトでゲーム画面0件。
  原因未確定のがたつきを直したとは書かない。
- 小イラストの次段階はメニュー、ショップの商品、天候、ゲーム内小物。
  Claude側の既存PNG・Canvas描画と新しい未公開素材を再点検し、使えるものを活かす。
  初回の空の皿アイコンはまだ未接続。全絵文字・履歴を置換済みとは扱わない。
- 顔の差分は将来の代表キャラから検討。承認済み原画と顔のない卵を維持する。

通常／特別／深海デートと終了ボタンは既存OK、記念日は「ひとまずOKとして進める」を保持。
同じ確認をユーザーへ依頼する段階へ戻らない。9伝説の残る実機確認は未完了。
PR #183は公開済み、#204は#205へ統合してclose済み。古いDraft指示やBS・BP・BOへ戻さない。
旧#92も独断で統合しない。
