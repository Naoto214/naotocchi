# Phase 4E-2 — home↔forest を ほんとうに あるく(Canvas の PoC)

日付: 2026-09-23 ／ 対象: `meguru.js` ／ 前提: [Phase 4E 設計](../design/meguru-phase4e-continuous-corridor-world-2026-09-23.md)(#335)・[Phase 4E-1](meguru-phase4e1-corridor-geometry-2026-09-23.md)(#336、main `6191f76`)

**`home|forest` の 1 本だけ**、地域の出口から となりの地域の入口まで、暗転の演出(transition)ではなく **その道を自分で歩いて** こえられるようにした。
ほかの 12 本(walk 9 本・ふね・ゴンドラ・もぐる)は 今の transition のまま。save の形・`travelToRegion()`・世界地図・分母は変えていない。

---

## 0. やったこと / やっていないこと

| | |
|---|---|
| **足したもの** | `CONTINUOUS_WALK_ALLOWLIST`(= `['home\|forest']`)／ `continuousWalkMode()` ／ `createCorridorWalk()` ／ `corridorDistantBlend()` と、その下の chart・合成 world・当たり判定(4E-1 ブロックのすぐ後ろの 1 ブロック)。`start()` の中の glue(`frameFn` の前の 1 ブロック + 印の付いた 3 行) |
| **変えたファイル** | `meguru.js` ／ `tests/meguru-phase4e2-test.cjs`(新規 19 本)／ `tests/meguru-phase4b・4c・4d1・4d2・4e1-test.cjs`(「消しても変わらない」テストが 4E-2 の印も消すように)／ `package.json`(テスト登録)／ `index.html`(`npm run bump`) |
| **さわっていない** | simulation(`createSim`)・renderer(`createCanvasRenderer`)・`TRANSITION` と `transitionPlan()`・`travelToRegion()`・save の key と形・世界地図・分母・gate の数(テスト 18 / 19 で縛る) |
| **やっていない** | ほかの 9 本への展開 ／ special connection(ふね・ゴンドラ・もぐる)の連続化 ／ streaming・preload の本実装 ／ global collision ／ resident の地域間移動 ／ save schema の変更 ／ Three.js |

> あとから たした §16 の party formation(なかまの ならび)は めぐる 全体の ものなので、4E-2 の 印の そとに ある(`partyFormationSlots` と region の `followParty` / `placeParty`)。remove-it テストの 対象外。

4E-2 のコードは全部 `// ====== Phase 4E-2: … ======` 〜 `// ====== /Phase 4E-2 ======` のブロックか、行末 `// Phase 4E-2` の行にある。
消すと 4E-1 までの動きに完全にもどる(4B/4C/4D-1/4D-2/4E-1 の remove-it テストが、4E-2 の印も消したうえで緑)。

---

## 1. いつ corridor を歩くか(`continuousWalkMode`)

gate が出たとき(`beginTransition(g)` の頭)に 1 回だけ判定する。`'corridor'` 以外は **今の transition**。

| 理由 | 結果 |
|---|---|
| `g.way !== 'walk'`(ふね・ゴンドラ・もぐる・のぼる) | `not-walk` → transition |
| `CONTINUOUS_WALK_ALLOWLIST` に無い(walk ほか 9 本) | `not-allowed` → transition |
| 4E-1 の spec が無い / 形が合わない | `no-spec` / `geometry-invalid` → transition |
| よいやすい設定(`prefers-reduced-motion`) | `reduced-motion` → transition(4E-1 `corridorMode`) |
| `perfTier` 2 | `perf-tier` → transition |
| この セッションで しっぱいした(`corrFailed`) | `failed` → transition |
| `createCorridorWalk()` が投げた(state / geometry が作れない) | `corrFailed` に入れて transition |
| 着く がわの `buildWorld` が投げた | 出発地域の出口へもどる(save はそのまま)。次から transition |

`home.bigtree` には `forest` と `river_lake` の 2 つの gate がある。**`river_lake` は `home|river_lake`(許可リスト外)なので corridor にならない**(テスト 15、実画面でも確認)。

許可リストに 1 本足せば、その corridor が歩けるようになる形にしてある(4E-3 で使う)。

---

## 2. CorridorState(save しない)

`createCorridorWalk(spec, from, opts).state` の key:

`connectionId` `fromRegion` `toRegion` `s` `u` `direction`('forward' / 'reverse') `speedMultiplier` `firstVisit` `width` `active`

- **正本は `s`(弧長 0〜walkLength)と `u`(横ずれ)**。chart の x/z は `chartPose(ch, s, u)` で毎回出す
- save には 1 文字も入らない(テスト 11 で `lifetime.meguru` を見る)
- `regionId`(save)は **着いた frame に 1 回だけ** 書きかわる。歩いているあいだは出発地域のまま
- 途中で reload / もどる → 出発地域から(テスト 12、実画面 `reload` / `home-button`)

---

## 3. geometry(corridor 専用の一時 chart)

- chart = **出発地域の local 軸**にそろえた平面。原点は出口 spot + `CORRIDOR_ORIGIN`(20000)。地面の模様が負の座標を読まないため
- 向き = 4E-1 の `corridorHeadingAt()`(global)− 出発地域のずれ `off(r) = endpoints[r].leaveHeadingGlobal − leaveHeadingLocal`
- 10 ごとに積分した `Float64Array` の xs / zs / hs を持つ(2700 → 271 点)。端から先はまっすぐ延ばす
- 右むき = `(cos h, −sin h)`
- 逆向き(forest → home)は同じ spec を逆に読む。段のことばは forest がわ(`いえなみ` まで)

| home\|forest | 値 |
|---|---|
| 段 | 6(urban-edge / field / field / forest / forest / forest) |
| walkLength | 2700(段 × 450) |
| 幅 | wide 520(uMax 238)× 3 段 → normal 380(uMax 168)× 3 段 |
| 曲がる量 | 合計 75.2°(段ごと 2.3 / 16.4 / 18.8 / 18.8 / 16.4 / 2.3) |
| 所要 | 初回 10.40 秒(spec 10.38)／ 再訪 7.43 秒(spec 7.42、× 1.4) |
| 段のことば(home→forest) | いえなみの はずれ / はたけ / かじゅえん / ざつぼくりん / きが ふえる / もりの いりぐち |

---

## 4. 動き

- **入力は道に対して**。指を置いた瞬間の `cos(camera.yaw − h)` で前後の向きを決め、離すまで固定(カーブで止まらない)。`fwd = −y·dir`、`side = x·dir`
- 速さ = いつもの歩き × `speedMultiplier`(初回 1.0 / 再訪 1.4)
- **初回か** は 今ある はっけんの記録(`S.worldLinks()` + `worldLinksFrom(allDiscoveredSpots())`)と、このセッションで こえたか(`crossedBefore`)。新しい save key は作っていない
- `s < 0` → 出発地域の同じ出口へもどる(commit しない)。`s ≥ walkLength` → 着く
- `u` は段の `uMax` で止まる(レールではなく、帯の中は左右に自由に歩ける。テスト 5)

### むき(yaw の連続性)

- 入った瞬間はカメラを動かさない(入る前の yaw をそのまま使う)
- カメラは遊び(dead zone)なしで道の向きに指数で追う(`CORRIDOR_CAM_FOLLOW` = 3/s、上限は いつもの `RULES.cam.turnRate`)
- 着いたときは `yawIn(to)` で 着く地域の local に直して `carry` で渡す。いつもの `enterWorld` の carry と同じ仕組み

| | 初回 | 再訪 |
|---|---|---|
| カメラの曲がる速さ(最大) | 10.9°/s(spec 10.9) | 15.2°/s(spec 15.2) |
| 入った瞬間の yaw の差 | 0° | 0° |
| 着く瞬間の道とのずれ | 0.04° | < 1° |

(テスト 6。はじめの実装は遊びありで 71°/s の段つきだったので、遊びなしに直した)

---

## 5. renderer(いまの `createCanvasRenderer` をそのまま使う)

renderer は 1 行も変えていない。corridor 用の **軽い合成 world** を作って、いつもの `draw()` に渡す。

| 中身 | 数 |
|---|---|
| 道(segments) | 90 ごと 30 本 |
| 地面の しるし(marks) | 96 |
| 飾り(props) | 段ごと 14 → 90(上限 `CORRIDOR_SCENE_MAX` 150)。段の地面の種類で表 `CORRIDOR_SCENERY` から選ぶ |
| 遠い景色 | 今の遠景(4D-2b)。`corridorDistantBlend` で t < 0.4 出発がわ / 0.4〜0.6 両方 / > 0.6 着くがわ。着くがわの方位は `offTo − off` で直す |
| backdrop / sky / detail / wind / motion / edge | t = 0.5 で出発 → 着くがわに切りかえ(今ある種類だけ) |
| 地面・道の色 | `mixRgb` + smoothstep で home → forest になめらかに |
| canopy | t > 0.75 で着くがわ |
| 住民 | 0 |
| JSON の大きさ | 約 22 KB(上限 200 KB) |

- 実画面: stage 0 = いえなみ、stage 2 = むぎ畑と木、stage 4 = 森と treeline の backdrop(3 サイズで撮影)
- 遠景の更新は t × 40 で間引く(1 本で 40 回)

---

## 6. 当たり判定(corridor だけ)

- 段ごとに いし(🪨)1 こ、帯の はしに(`u = side·(uMax − 16)`、r = 24)。1 本で 6 こ
- 押し出しは (s, u) の空間でやり、そのあと もう一度 `u` を帯に止める
- **region の当たり判定と同時には持たない**。corridor にいるあいだ sim は step しない(出発地域の world は sim に残したまま動かさない)。着いたときに `enterWorld` が forest の当たり判定を作り、corridor のものは捨てる
- テスト 7: いしに めり込まない・帯から出ない・region の collider を読まない

---

## 7. なかま(party)

- corridor に入るとき、今の party の **同じ object** を chart に移して ついて来させる(新しく作らない。テスト 8)
- 引き返し: `restoreParty()` で出発地域の local にもどす
- 着いた: `enterWorld` がいつもどおり party を作り(1 組だけ)、じぶんの すぐ後ろに並べる。transition で着いたときと同じ顔ぶれ(テスト 14 で比べる)
- 住民は 0。はなす・のる の UI は出さない(`setAct(null)`・`nearest: null`)

---

## 8. UI

| ボタン | corridor 中 |
|---|---|
| たび | 使えない |
| ちず | 使えない |
| もどる | 使える(めぐるを出る。save は出発地域) |
| ヒント | `【段のことば】もりの ほうへ` |
| spot のチップ・しらせ | かくす(着いてから出す) |

3 サイズで pad / もどる のはみ出しは 0(padOut / homeOut −33〜−36、overflow 0)。

---

## 9. 受けわたし(handoff)

| | どうするか |
|---|---|
| 入る | 出発地域の え の上に 0.1 秒だけ暗くして corridor へ(`CORRIDOR_COVER.fadeIn`) |
| 着く | 0.1 秒暗く → **その frame で** `enterWorld(to, {at, heading, carry, quiet})` → 成功したときだけ `S.enterRegionByMove(to, {by: 'walk'})`(**commit はここ 1 か所**)→ `sim.setPlayer(pose)`・party を並べる → 0.14 秒で明ける |
| 着くがわが作れない | `corrFailed` に入れて出発地域の出口へ。save は変わらない。次からは transition(テスト 16) |
| 引き返す | `backPose()`・`restoreParty()`・`sim.setPlayer`・`yawIn(from)`・遠景をもどす。出発地域は作り直さない |

transition は「save を書いてから `enterWorld`」の順。corridor は「`enterWorld` が成功してから save」の順にした(作れなかったときに save だけ進むのをふせぐ)。
companion の `region` は save の `regionId` を見るが、**着いた地域の party は `withPlayer` だけで決まる** ので顔ぶれは変わらない(テスト 14)。

---

## 10. 性能(実アプリ、390×844・DPR 3・**CPU 4 倍おそく**・2 回)

rAF 1 回の時間と、実際に えがいた frame の 2D 命令数(`fill` `stroke` `fillRect` `drawImage` `fillText` `arc` `ellipse` `create*Gradient`)。

| 場面 | p50 | p95 | 最大 | 2D 命令 p50 / 最大 |
|---|---|---|---|---|
| home を歩く(基準) | 5.7〜10.9 ms | 30.8〜36.4 ms | 49 ms | 1985 / 2085 |
| **corridor を歩く** | **7.2 ms** | **12.6〜14.2 ms** | 37.8〜51.4 ms | **1220** / 1558 |
| 段の切りかわり | 13.1〜15.7 ms | 18.2〜19.1 ms | 26.2 ms | 1281 / 1455 |
| 入る(暗くする間) | 5.0 ms | 8.8 ms | 8.9 ms | 362 / 745 |
| **着く frame(forest を作る)** | 113〜116 ms(暗転の下) | | | 1204 |
| 引き返す frame | 6.6〜7.8 ms | | | 1212 |
| forest を歩く(着いたあと) | 9.3〜10.9 ms | 31.1〜34.2 ms | 131〜166 ms | 2822 / 2947 |
| 参考: transition で着いた forest | 3.7 ms | 34.6 ms | 249 ms | 2862 / 3350 |

- 目標「p95 の悪化 ≤ +4 ms」: corridor は home を歩くより **p95 が 16〜22 ms 軽い**(悪化なし)
- 目標「2D 命令 ≤ +100」: corridor は home より **約 765 少ない**
- 目標「forest を作る山 desktop < 100 ms」: おそくしない実測で `lastBuildMs` 16〜31 ms。4 倍おそくして 113〜116 ms(暗転の下で見えない)
- 目標「暗転 ≤ 0.35 秒」: 暗くする 67 ms + 作る 113〜116 ms + 明ける 140 ms = **320 ms**(4 倍おそくして)。はじめは 0.12 / 0.2 秒で 450 ms だったので 0.1 / 0.14 秒に縮めた
  - **訂正(§15)**: この 320 ms は 見つもりの まちがい。あけの 0.14 秒を 1 まいで おわると みなし、さいしょの え(113〜166 ms)を 足して いなかった。じっさいに はかった 暗転(くらく なりはじめ 〜 あけおわり)は **forest 745〜908 ms / home 589〜717 ms** だった
- 目標「scene < 200 KB」: 約 22 KB
- いちばん重い frame: corridor 中は 51.4 ms(1 回。GC と思われる)、段の切りかわりは 26.2 ms
- JS エラー 0

---

## 11. iPhone 3 サイズ(Playwright、DPR 3)

390×844 / 375×667 / 360×640 × 5 場面 = **15 / 15 PASS**、JS エラー 0。

| 場面 | 確かめたこと |
|---|---|
| 往復 | home → forest(初回 × 1.0、firstVisit true、party 2、段 0〜5、歩いている間の save は home)→ forest 入口に着く ／ forest → home(× 1.4、約 7.1〜7.2 秒)／ U ターン(段 0〜2 で引き返して forest の入口へ)／ home.bigtree から river_lake へは corridor なし |
| reload | 途中で reload → home、corridor は残らない |
| もどる | 途中で もどる → save は home |
| reduced motion | 今の transition で forest へ |
| perfTier 2 | 今の transition で forest へ |

スクリーンショット: 3 サイズ × stage 0 / 2 / 4 + 着いたあと。

---

## 12. テスト(`tests/meguru-phase4e2-test.cjs`、はじめ 19 本 → 安定化で 25 本。§15.8)

1. corridor は home\|forest(行き・帰り)だけ。ほか 9 本は transition、special は `not-walk`
2. fallback の理由(reduced / perfTier 2 / failed / 形)。perfTier 0・1 は corridor
3. home → forest: 初回 10.4 秒 / 再訪 7.4 秒、段 0 → 5、forest の入口へ
4. forest → home: 同じ形を逆に、ことばは forest がわ
5. `u` は帯の中だけ(レールではない)
6. yaw: 入る瞬間 0、最大は spec どおり、着くときのずれ < 1°
7. いしの当たり判定(1 段 1 こ、めり込まない、region の collider を持たない)
8. なかまは同じ object で ついて来る。住民 0
9. 引き返し: 出発地域の同じ出口へ(commit なし)
10. 飾り ≤ 150、地面の種類、地面の色、遠景のまざり
11. `start()`: 歩いている間の save は home、着いたら forest。え は home → corridor → forest。帰りも歩ける
12. 途中 reload → 出発地域から。save の形は同じ
13. `start()` で引き返す → home の大きな木へ
14. reduced / perfTier 2 → transition。着いたあとの なかまは corridor のときと同じ
15. home.bigtree の river_lake gate は corridor にならない
16. forest が作れない → home の出口へ、save は home、次からは transition
17. countryside → forest は transition
18. 4E-2 のコードは印の付いたところだけ。simulation・renderer・TRANSITION・travelToRegion は同じ
19. 分母・spot・たび・save・世界地図・corridor の数(471 / 654 / 118 / 107、11 / 12 / 17 / 103、connection 14、gate 13)は同じ

わざと壊して落ちることも確かめた: 許可リストに river を足す ／ commit を入るときにする ／ `u` を止めない ／ reduced を見ない → どれも落ちる。

`npm test`: **1285 / 1285**。

### 前からある ゆらぐテスト(4E-2 とは関係ない)

`meguru-discovery-test` の ⑥-1 と `meguru-test` の「entering めぐる…」が、`Math.random` の種を固定していないため まれに落ちる。
origin/main(4E-2 の前)でも同じ 2 本が落ちることをローカルで再現した。main `6191f76` の Runtime smoke は 1 回目 1264 / 1266(2 本落ち)で、同じ commit の再実行で緑になった(コードの問題ではなく ゆらぎ)。
4E-2 のブランチの全体実行ではこの回は出なかった。直すのは別の PR(今回の範囲の外)。

---

## 13. PoC の判定(はじめ): **修正して採用** → §15 の 安定化の あとの 判定を 見る

**しくみ(一時 chart・s/u が正本・commit は着いたとき 1 回・fallback は今の transition・合成 world を今の renderer に渡す)は そのまま採用**。
性能・save・yaw・party・当たり判定・3 サイズの画面は、どれも目標の中に入った。

ただし ほかの 9 本に広げる前に、次を直す(= 4E-3 に進む条件):

1. **着く frame の山**: 4 倍おそくして 113〜116 ms。暗転の下なので見えないが、暗転の長さ(320 ms)は目標 350 ms まで 30 ms しか余裕がない。もっとおそい端末で暗転が のびる。着く地域の `buildWorld` を最後の段のうちに分けて始める(preload の最小版)か、暗転の中で 2 frame に分ける
2. **段の切りかわり**: p95 18〜19 ms(歩いている時の p95 の約 1.4 倍)。backdrop / sky の切りかえと遠景の入れかえが同じ frame に重なる。frame をずらす
3. **けしきの質**: 飾りは段の地面の種類ごとの小さな表だけ。ほかの 9 本(雪・砂・川・街)は表を足すだけで出るが、見た目の確認は 1 本ずつ要る
4. **main の ゆらぐテスト 2 本**: `Math.random` の種を固定する(CI が赤くなると 4E-3 の判断ができない)

---

## 14. 4E-3 への入力(まだ やらない)

- 許可リストに足す順番の案: 形が やさしい順(bend が小さく、幅が wide / normal)。narrow・bend 170° 超えは最後
- 足すときは 1 本ずつ: spec → 画面 3 サイズ → 性能(4 倍おそく)→ 往復・引き返し・reload
- special connection(ふね・ゴンドラ・もぐる)・global collision・resident の地域間移動・save schema・Three.js は 4E-3 でも範囲の外(4E 設計 §22 のまま)
- 自動 check-in は設定していない

---

## 15. 安定化(#337 に 追加。2026-09-23)

のこって いた 4 つ(ゆらぐ テスト・着く frame・段の 切りかわり・けしき)だけを 直した。ほかの 9 本・streaming / preload の 本実装・global collision・住民の 地域間移動・Three.js には すすんで いない。

### 15.1 main で ゆらぐ テスト 2 本(テストだけ 直した。本番の らんすうは そのまま)

| テスト | 原因(しらべて わかった こと) | 直しかた |
|---|---|---|
| `meguru-discovery-test` ⑥-1 | **実時刻の じかん・てんき だけ**。`weatherMode: 'auto'` の てんきは 日付 + 3 時間ごとの わく の ハッシュで、「ひる × はれ」の ときだけ きのこ(`form:mushroom:0`)が ひだまりの 107 さきに 来て「はなす」が 出る。らんすうは 関係ない(たね 160 とおりで 同じ) | この テストだけ じかん `day`・てんき `cloudy` に とめる |
| `meguru-test` entering めぐる | **実時刻の じかん・てんき × たね なしの らんすう**。「ひる × はれ」(約 32%)「あさ × ゆき」(44%)「ゆうがた × くもり」(80%)で くまが きのこの そばへ 来て、どちらが ちかいかが らんすう しだい | じかん・てんき を とめ、めぐるの らんすうに たね(`setRandom(seededRandom(20260923))`、#327 と おなじ しかた) |

- 住民の 初期配置は じかん・てんき で きまる(らんすうでは ない)。日付(`dailyKey`)は 366 日 ずらしても 影響なし。順番の 依存も なし(テストごとに ハーネスを 作りなおす)
- 2026-09-23 11:23 UTC の main CI が 赤 → 12:26 に 同じ commit の 再実行で 緑 だったのは、その あいだに 3 時間の わく が かわって てんきが「はれ」→「あめ」に なった ため
- **修正前**: 実時刻だけを 11:23 UTC に ずらして(preload で `Date` だけ ずらす)本物の テストファイルを 20 回ずつ → ⑥-1 **20/20 赤**、entering **9/20 赤**。いまの 時刻では どちらも 20/20 緑
- **修正後**: 11:23 UTC で 50/50、いまの 時刻で 50/50(どちらも)。さらに 3 日 × 3 時間ごと 8 わく(24 とおり)で 24/24。entering は たね 300 とおり、⑥-1 は 60 とおりでも 緑
- 2 本の 原因は ちがう(⑥-1 は 時刻だけ、entering は 時刻 × らんすう)ので、それぞれ 最小の 直しに した(commit `b33947f`)。4E-2 とは 関係ないが、main へ さきに 入れる ひつようまでは ない(たまに 赤く なる だけ)ので #337 に のせた

### 15.2 着く ときの 山: 中身

実アプリ・CPU 4 倍・390×844・DPR 3 で 分けて はかった(3 回)。

| 中身 | forest に 着く | home に 着く |
|---|---|---|
| 住民の 台帳(`buildRegistry`) | 0.6〜1.2 ms | 0.7〜0.9 ms |
| **`buildWorld`** | **87〜115 ms** | 11〜14 ms |
| なかま(`companionsOf`)・のこり | 1〜3 ms | 0〜1 ms |
| ちずの きろく・絵の 読みこみ予約・遠景・UI | 5〜8 ms | 1〜3 ms |
| commit(`enterRegionByMove`) | 2.5〜2.9 ms | 0.8〜1.3 ms |
| **さいしょの え**(1 まいめ) | **126〜178 ms**(そのうち `drawImage` 102 ms = 絵を はじめて 作る 立て看板) | 35〜78 ms |

- **corridor だけの 山は ない**: いまの transition で forest に 入っても `buildWorld` 87〜115 ms・さいしょの え 131〜171 ms(forest の もとからの おもさ)。corridor で ふえた ものは ない
- 見つもり(§10 の 320 ms)は まちがいで、じっさいの 暗転は forest 745〜908 ms。原因は ①くらく する / あける を frame の dt(最大 0.05 秒)で すすめて いた ので、おもい frame が つづくと のびる ②着いた つぎの frame が dt = 0 で むだに なる ③さいしょの え が 暗転 あけの とちゅうに はいる

### 15.3 着き方: prepare → commit → show

| 段 | なにを するか | どこで |
|---|---|---|
| **prepare** | `buildRegistry()` + `buildWorld(着く 地域)` を 組んで おく(地域も セーブも かえない) | 終端の **8% てまえ**(2700 × 0.08 = 216、はじめて 0.8 秒 / 2 かいめ 0.6 秒 まえ) |
| したく | 組んだ world の 立て看板の 絵を、見えない 4×4 の canvas で 8 回に わけて 先に 作る(絵は 地域を こえて つかいまわされる) | prepare の つぎの frame から |
| くらく する | 終端の てまえ(あるいて 0.06 秒の ぶん)から **s に あわせて** くらく する。とまれば そのまま、もどれば あかるく | あるき ながら |
| **commit** | `enterWorld` → `buildWorld` は prepare の world を **1 回だけ** うけとる(`takeCorridorWorld`。台帳・まち・じかん・てんき・きせつ・日付が 同じ ときだけ)→ 成功した ときだけ `enterRegionByMove` | 終端に 着いた frame |
| **show** | さいしょの え を くらい まま えがく | 同じ frame |
| あける | じっさいの じかんで 0.12 秒 | つぎの frame から |

- **二重 build なし**: prepare 1 回 → commit で hit 1 回(テスト 21 / 実画面で `handoff.hits`)
- **しっぱい**: prepare で こけた → 着いた ときに 出発 地域へ(テスト 16)。commit で こけた → 地域が かわる まえ でも かわった あと でも 出発 地域を 組みなおして もどる(テスト 22、実画面 build-failure)。どちらも つぎからは transition
- **引き返し・もどる・reload**: 組んだ あとで 12% いじょう もどったら すてる。もどる / reload は すててから おわる(組んだ ものは メモリだけ)。forest へ commit しない(テスト 23、実画面 prepared-back)
- **帰り(forest → home)も 同じ しくみ**(home だけの とくべつな みちは ない)
- **メモリ**: 組んだ world は forest 約 466 KB / home 約 101 KB(ヒープ)。持つのは 92% 〜 着く まで(1 秒 いない)で、着いたら その まま いまの world に なる(2 つ もたない)。4E-3 に まわす ほどの 大きさでは ない

### 15.4 A / B / C の くらべ(CPU 4 倍、forest に 着く)

| | いつ 組むか | 暗転 | あるいて いる あいだの いちばん おもい frame |
|---|---|---|---|
| まえ(4E-2) | 暗転しきってから(dt で あける) | 745〜908 ms | 54〜77 ms |
| A | 暗転しきってから(じっさいの じかんで あける) | 667〜700 ms | 63〜77 ms |
| C | くらく なりはじめの frame | 717〜767 ms | 60〜63 ms |
| B | 終端の 4% てまえ | 550〜667 ms | **122 ms**(組む frame) |
| **B + したく + くらく なりながら 着く(採用)** | 終端の 8% てまえ | **300〜483 ms**(4 回の なかで 中央 約 420) | 組む frame 98〜113 ms |

- C は 組む じかんが 暗転の なかに のる ので みじかく ならない。B は 暗転は みじかく なるが、組む frame が 見えて いる あいだに 1 回 ある(4 倍おそくして 約 100 ms、おそく しないと 約 25 ms)
- したく(絵を 先に 作る)で さいしょの え は **126〜178 → 55〜77 ms**、着く frame は **238〜252 → 66〜90 ms**
- はじめは したくを ちかい じゅんに くばって 1 回目が 135〜218 ms に なった → n こ おきに くばり なおし、とても 大きな 絵(bigtrunk)を corridor の 飾りから はずして、したくの 1 回は **最大 48 ms** に なった

### 15.5 段の 切りかわり

- 切りかわりの frame で おもく なって いたのは **え**(renderer の draw)。ヒントの 文字を かえる と、その あとの canvas の 字 の 計算で スタイルの 計算が はしる → **段の なまえは え を えがいた あとで かえる** ように した
- **遠景が 後半で 出て いなかった**(まえの PoC の バグ): corridor の world は まんなかで 到着 がわの 地域に なるが、遠景は 出発 がわの 地域の まま わたして いた ので、renderer が「地域が ちがう」と して 後半の 遠景を えがいて いなかった。いまの world の 地域で わたす ように 直した(t < 0.4 出発 / 0.4〜0.6 両方 / > 0.6 到着 は その まま)
- 段ごとの みため(地面の いろ・飾り)は corridor を 作る ときに 1 回だけ きめ、毎 frame は まぜる わりあい だけ(同じ なら なにも しない)。段の 切りかわりで 配列を 作りなおさない・らんすうを 使わない・DOM を 組みなおさない

### 15.6 けしき(4E-1 の terrain に そって)

| 地面の 種類 | いろ(かりる 地域) | みちの そば | おく | 山なみ |
|---|---|---|---|---|
| urban-edge(いえなみ) | home | さく(fence)・🌷・🌼・🪴 | 🏠 🏡 🌳 | hills |
| field(はたけ) | countryside | うね(cropline)・🌾・🌻 | 🌳 🏡 ほしくさ(hayroll) | farhills |
| forest(もり) | forest | 🌿 🍄 🪵 🍂 しだ(fern) | 🌲 🌳 | treeline |

- **いまの 地域の いろと 飾りの ことば だけ**(あたらしい 絵は ない。テストで 全部 どこかの 地域に ある ことを たしかめた)。飾りは 96 こ(上限 150)
- 段の さかいは **段の 30%(= 全体の 5%)の はばで まざる**: 飾りの ことばは となりの 段の ものが まじり、地面・みちの いろは なめらかに かわる(10 ごとの いろの とびは 6 いない)
- 森の 屋根(canopy)は 森の おく(森で ない 段から 2 段 はなれた ところ)だけ。着く ちょくぜんに 出発 がわの 飾りは のこらない(forest の いりぐちに いえは ない / home の いりぐちに 森は ない)
- はじめて でも 2 回目 でも けしきは おなじ(はやさ だけ かわる)。石は 1 段 1 こ の まま、みちの まんなかから はなれて いる
- 0 / 15 / 30 / 45 / 60 / 75 / 90 / 100% を 行き・帰りで 撮って 見なおした。まえの PoC では 家 → 森 の あいだが「森が うすく なる だけ」だったのが、いえなみ → はたけ(うね・ひまわり・ほしくさ)→ 森 と 見て わかる。しらべて いる とちゅうで、とても 大きな 木の みき が 家の 段から 見えて いた ので はずした

### 15.7 UI の のこり(再発)

- **見つかった**: 帰り(forest → home)で、こえはじめた frame に 出た「あかるいもりに きた」の しらせ と「はなす」ボタンが corridor の あいだ ずっと 残って いた。こえはじめた frame で `beginTransition` の あとも その frame の のこり(ボタン・しらせ・ヒント)が うごいて いた ため
- **直した**: こえはじめた frame は イベントの あと すぐ もどる(印の ついた 1 行)
- テスト 20(corridor の あいだ しらせ・ボタン なし、ヒントは corridor の もの)と、実画面で 毎 0.1 秒 DOM を みる(しらせ・はなす・場所の なまえ が 出て いない、たび・ちず 使えない、もどる 使える)

### 15.8 テスト

`tests/meguru-phase4e2-test.cjs` は 19 → **25 本**(10 は けしきを 書きなおし、20〜25 を 追加)。

| 番号 | たしかめる こと |
|---|---|
| 10 | 段ごとの 飾りの ことば(さかいの はばの そとは その 段の もの)、いえなみ / はたけ / もり の ちがい、地面の いろは その 地域の いろで なめらか、山なみ、屋根、帰りは さかさ |
| 20 | corridor の あいだ しらせ・その ばの ボタン・地域の ヒントが のこらない(行き・帰り) |
| 21 | 終端の てまえで 1 回 組み、着いた ときに それを つかう(hit 1、miss 0)。帰りも 同じ |
| 22 | commit で こけた(地域が かわる まえ / あと)→ 出発 地域・セーブは home・つぎは transition |
| 23 | 組んだ あと 引き返す(すてる)/ 暗く なりかけて 引き返す / 組んだ あと もどる → forest へ commit しない |
| 24 | けしきは 1 回目も 2 回目も 同じ、石は みちの まんなかに ない |
| 25 | なかまは 段の さかいでも とばない・きえない |

- わざと こわして 落ちる ことも たしかめた: prepare で commit する / handoff を つかわない / 引き返しても すてない / こえはじめた frame で もどらない → どれも 落ちる
- 4E-1 の テスト 15(「4E-1 は まだ だれも つかって いない」)は、4E-2 の 印の ところを のぞいて しらべる ように した(ほかの remove-it テストと 同じ)

### 15.9 さいごの 性能(CPU 4 倍・390×844・DPR 3、4 回。おそく しない は 2 回)

| | 9f14474(まえ、同じ しかたで はかり なおし) | いま |
|---|---|---|
| corridor を あるく p95(行き / 帰り) | 12.7〜14.6 / 14.5〜17.4 ms | 14.8〜17.3 / 15.9〜18.1 ms |
| 段の 切りかわり p95 | 15.8〜19.4 / 14.2〜19.8 ms | 12.9〜21 / 11.5〜16.1 ms |
| 2D 命令 p50(あるく、行き / 帰り) | 1099 / 1004〜1334 | 1270〜1285 / 1588 |
| 2D 命令: 切りかわり − あるく | +148 / +4 | **+3 / −433** |
| home を あるく ときの 2D 命令(くらべ) | 1985 | ― |
| **暗転(forest に 着く)** | 745〜908 ms(§15.2) | **300〜483 ms** |
| **暗転(home に 着く)** | 589〜717 ms | **267〜317 ms** |
| 着く frame | 238〜252 ms | 66〜90 ms |
| さいしょの え | 126〜178 ms | 55〜77 ms |
| 組む frame(あるいて いる あいだ) | ― | 98〜113 ms(forest)/ 15〜22 ms(home) |
| したく の 1 回(最大) | ― | 48 ms |
| いちばん おもい あるく frame | 54〜71 ms | 112〜139 ms(corridor の **さいしょの 1 frame**。入口の 暗転の した) |
| おそく しない: 暗転 forest / home | ― | 217〜233 / 200 ms |
| おそく しない: 組む frame | ― | 25〜27 ms / 3〜4 ms |

- あるく p95 は まえと ほぼ 同じ はば(1〜2 ms 上。まえ の はかり なおしでも 12.7〜17.4 と ゆれる)。2D 命令は home より すくない
- 段の 切りかわりは あるく p95 + 3 ms いない(4 回中 1 回だけ +3.7)。2D 命令の さは +30 いない
- 暗転は **home は 目標(≤ 350 ms・300 ms 前後)に 入った**。**forest は 中央 約 420 ms で 350 ms を こえる ことが ある**。のこりは forest の え 1 まいの 重さ(4 倍おそく・DPR 3 の ソフト描画で frame の 間が 100〜180 ms。JS は 着く frame 66〜90 ms)で、あける 0.12 秒が 着いた つぎの 1〜2 frame に またがる ため。おそく しないと 217〜233 ms

### 15.10 iPhone 3 サイズ(390×844 / 375×667 / 360×640)

**24 / 24 PASS**(8 とおり × 3)、JS エラー 0。

| とおり | 見た こと |
|---|---|
| 往復 | ①行き(はじめて × 1.0)②帰り ③2 回目(× 1.4)④U ターン ⑤帰り ⑥river_lake は corridor なし。なかまは corridor でも 着いても 同じ かず。**UI の のこり 0**(0.1 秒ごとに DOM を みた) |
| 段の さかい | 450 ふきんで 横へ(u ≈ 92、帯の なか)、900 ふきんで 引き返して また すすむ → forest |
| 組んだ あと 引き返す | 92% で 組んだ あと もどる → home の 出口、すてた 1、forest の world は つかって いない |
| 着く ときに こける | commit の とちゅうで こけさせる → home、セーブ home、つぎは transition で forest |
| reload / もどる | 出発 地域から、セーブに corridor の あとかた なし |
| reduced motion / perfTier 2 | いまの transition |

### 15.11 かわって いない もの

save の 形(あたらしい key なし。組んだ world も 段の みため も メモリだけ)・`travelToRegion()`・世界地図・分母・数(471 / 654 / 118 / 107、11 / 12 / 17 / 103、connection 14、gate 13)・ほかの 9 本と ふね / ゴンドラ / もぐる の transition(テスト 1 / 17 / 19)。simulation と renderer の 中は かえて いない(`buildWorld` の いりぐちに 印の ついた 1 行、`stop()` に 1 行、`frameFn` に 1 行)。

### 15.12 判定(安定化の あと): **修正して採用**(Draft の まま)

- 直った: ゆらぐ テスト(原因を 特定して テストだけ 固定)、着く ときの 二重 build なし・しっぱい / 引き返し / もどる で 安全、段の 切りかわり(p95・2D 命令)、けしき(いえなみ → はたけ → 森 が 見て わかる・さかいは 5% で まざる)、後半の 遠景、UI の のこり、3 サイズ
- **のこり 1 つ: forest に 着く ときの 暗転が CPU 4 倍で 300〜483 ms(中央 約 420)**。目標 ≤ 350 ms に いつも 入る とは いえない。home は 267〜317 ms、おそく しないと forest も 217〜233 ms
- **#337 は main へ マージしない**(§60 の じょうけん「PoC 判定 = 採用」を みたさない)

### 15.13 4E-3 へ すすめるか

**まだ**。forest の 暗転を 4 倍おそくして いつも 350 ms いない に する めどが たってから。考えられる 手:
1. 着いた frame の つぎ(forest の 2 まいめ)の え を 軽く する(暗転の あける あいだ だけ 遠くの 飾りを へらす など、renderer の がわ)
2. 組む(`buildWorld(forest)` 約 100 ms)を 2 frame に わける — forest の 組み立て じたいを 分ける ことに なる ので 4E-3 の streaming と いっしょに 考える
3. 目標の はかりかたを 実機(iPhone)で たしかめる — いまの 数字は ヘッドレスの ソフト描画で、frame の 間(100〜180 ms)の ほとんどが え を ラスタライズ する じかん

---

## 16. Party formation(なかまの ならび)

めぐる全体(region と continuous corridor)で、なかま・こいびとが「横一列」ではなく「じぶんの うしろに ゆるく あつまる 群れ」に なるように した。見た目と ついていく いち だけ。なかまの AI・住民の 地域間移動・なかま どうしの 当たり判定・セーブは さわって いない。

### 16.1 監査(実コード)

| | 中身 |
|---|---|
| region の 追従(`createSimulation` の `followParty`) | i 番目の なかまを 横に ±100 ×(1 + 0.9 × ⌊i/2⌋)、おくへ 50 + 30 × i。こいびとは 向いて いる 側の 反対。**人数が ふえると 横へ ひろがる 一方**(27 にんで 横 ±1180・おく 800) |
| corridor の 追従(`createCorridorWalk` の `followParty`) | 同じ 式 + 帯の はしで 止める(はしに たまって 1 列に なる) |
| 入った とき | `companionsOf()` が (0, 0) に 作り、そこから かけよって くる。corridor で 着いた ときは じぶんの まうしろに 60 + 30 × i で 1 列 |
| 「うしろ」の 意味 | もとの コードの ことば どおり「カメラから みて おく」。カメラは じぶんの 400 うしろ、足もとは 画面の 80% の 高さ。カメラがわへ 114 いじょう 出ると 画面の 下から 切れる |
| 人数の 上限 | 上限の コードは ない。なかまに なった ものは `state.companions` に のこる。**いまの データでは なかま 26(ふつう 18 + レア 8)+ こいびと 1 = 27**(`companionsOf(buildRegistry())` で たしかめた。テスト 1) |
| home の 画面 | なかまは ペットの 左右に こうごに、こいびとは となり(`layoutHomeCast`)。「まわりに ひろがる」考えと 合う。home の UI は かえて いない |
| なかま / こいびと | 同じ あつかい に した(こいびとを いつも となりに する などの あたらしい きまりは つくらない。もとの「向いて いる 側の 反対」も ならびの 一員に もどした) |

### 16.2 あたらしい ならび(`partyFormationSlots(count, ctx)`)

region と corridor で 同じ pure helper を つかう(corridor だけの とくべつな ならびは ない)。

| 人数 | かたち |
|---|---|
| 1 | ななめ うしろ(横 80・おく 60) |
| 2 | 左右の ななめ うしろ(±100・おく 60)。じぶんの まうしろは あける |
| 3〜4 | ちいさな V(2 段目は 1 段目より ひろくて おく。4 にん: ±100・おく 60 / ±150・おく 125) |
| 5 いじょう | 半円〜おうぎ(おくの 段ほど ひろく、段の なかでは まんなかほど おく。8 にん 3 段・おく 230、27 にん 4 段・おく 295) |

- side / back は world 単位(px では ない)。back は いつも + (カメラがわへ 出ない)
- はばの 上限: ひろい ところ 260。**corridor では いまの 段の 帯(4E-1 の widthClass → uMax × 0.9)**。wide 214 / normal 151
- 段: ひろい ところ 4 段まで、せまい ところ(はば < 180)8 段まで。段の あいだは 65(4 段で おく 最大 300、せまい ところ 440)。それでも はいりきらない ときは 段の なかを すこし こく する
- ひとりずつの ちいさな ちがい: 名前(key)から 横 ±12・おく ±12・ついていく はやさ 0.9〜1.1 倍。毎 frame かわらない(らんすうは つかわない)
- 並び順は party の じゅん(いつも 同じ)。ならびは 人数 × はば ごとに 1 回 作って つかいまわす(毎 frame 作らない)。O(n)
- カメラの むきで まわす。カメラは なめらかに まわる ので、引き返し・曲がり・段の さかいで ならびが とばない
- 地域に 入った とき(`enterRegion`)・corridor で 着いた とき は、その ならびの ばしょに そのまま おく(`sim.placeParty()`)。とおくから かけよらない・1 列に もどらない
- ついていく はやさ: `min(上限, きょり × 6) × (0.9〜1.1)`。まえの 3.5 より つよく し、あるいて いる ときの おくれを 260/秒 で 約 74 → 43 に した(1 段目が じぶんの まよこ〜まえへ ずれない)。corridor の 上限は 340 × 1.4 × `speedMultiplier`(2 かいめ からの はやあし 1.4 倍でも おいつける)
- 当たり判定は ふやして いない(なかま どうし も じぶんとも ぶつからない。じぶんの あるく みちは なかまが いても 同じ)

### 16.3 せまい corridor

- 帯の はばから ならびの はばを きめる(wide 段 214、normal 段 151)。27 にんで wide 4 段・normal 8 段
- せまく なった 段に はいると、そとの なかまは 1 秒ほどで なかへ よる(とばない。テスト 7)
- せまくても 1 本の 線には しない(どの 段にも 左右が ある。テスト 3)

### 16.4 さいだい 27 にん

- 画面の なかに いるか を renderer と 同じ 投影で たしかめた(iPhone 3 サイズ、§16.7)
- 27 にんでは なかまどうしが かさなる(いちばん ちかい あいだ 50)。全員 同じ ばしょ・一直線・画面の はしに かたまる は ない
- こく なっても はばは 上限 260 を こえない(§29 の とおり、ちらすより うしろの みつどを すこし 上げる)

### 16.5 性能(CPU 4 倍・390×844・DPR 3)

最後の 形(firstBack 60・gain 6)で とりなおした。1 回 ずつの 計測なので ±5 ms ほど ゆれる。

| 人数 | region を あるく p95 | corridor を あるく p95 | corridor の 2D 命令 p50 | 着く 暗転 |
|---|---|---|---|---|
| 1 | 26.7 ms | 24.1 ms | 1264 | 367 ms |
| 2 | 24.3〜29.2 | 19.7〜22.9 | 1266〜1268 | 267〜533 |
| 4 | 31.9 | 20.5 | 1274 | 500 |
| 8 | 26.3 | 22.9 | 1288 | 800 |
| 16 | 27.6 | 25.7 | 1315 | 1283 |
| 27 | 29.1〜31.4 | 28.6〜33.8 | 1352 | 1433〜2017 |
| 2(まえの ならび `a064967`) | 25.1〜28.7 | 18.0〜19.6 | 1220〜1231 | 267〜417 |
| 8(まえ) | 33.9 | 20.1 | 1243 | 600 |
| 16(まえ) | 30.8 | 19.4 | 1249 | 833 |
| 27(まえ) | 35.9〜37.0 | 20.6〜24.5 | 1283〜1301 | 1067〜1667 |

- **ならびの 計算 そのもの は 軽い**: `sim.step` 1 回(描画なし)が 27 にん で 0.064 → 0.077 ms(+0.013 ms)。ならびは 人数 × はば ごとに 1 回 作って つかいまわす
- **region は かわらない**(ゆれの はばの なか)。**8 にん までの corridor も ほぼ かわらない**
- **corridor の 16〜27 にんは p95 が +5〜10 ms**。まえの ならびでは 帯の はしに よって 画面の そとに いた なかまが、いまは 全員 画面の なかに いる ため(え の 数 そのものは おなじ。2D 命令 +50〜70)。「全員 見える」の ぶんの 重さで、ならびの 計算 では ない
- 着いた ときの 暗転は **まえの ならびでも 人数で のびる**(2 にん 約 0.3〜0.4 秒 → 27 にん 1.1〜1.7 秒)。27 にんでは いまの ほうが すこし ながい ことが ある(1.4〜2.0 秒)。commit(10〜14 ms)・さいしょの え(50〜70 ms)は まえと 同じ で、のびて いるのは 暗く なりはじめて から 着く までの あいだ の frame(全員 画面に いる ぶん 重い)。27 にん だけの ことで、8 にん までは まえと 同じ ぐらい

### 16.6 見た目の たしかめ(1 / 4 / 8 / 16 / 27 にん)

- region・corridor・着いた ところ を 人数ごとに 撮った(scratchpad `shots/party-final/sheet.png`)。どれも じぶんの うしろに ゆるく あつまる。1 列・全員 同じ ばしょ・画面の はしに かたまる は ない
- 1 にん: ななめ うしろ。2 にん: 左右の ななめ うしろ。4 にん: ちいさな V。8 にん: 3 段。16・27 にん: おうぎ。27 にんは こいが 画面の なかに おさまる(はば 200〜220 px / 画面 338 px)
- じぶんは かくれない(なかまは いつも じぶんより おく に いるので、じぶんの え の うえに かさならない)
- 住民は ならびに 入れて いない

### 16.7 iPhone 3 サイズ(390×844 / 375×667 / 360×640)× 27 にん

`pw4e2c` の 既存 8 場面 × 3 サイズ(24)に「27 にん」を 1 場面 × 3 サイズ たした。**27 / 27 PASS**。

| 場面 | 見た もの | 結果(3 サイズ) |
|---|---|---|
| 27 にん home → forest(段の さかいで 横・少し 引き返し) | 画面の そと / じぶんより まえ / 同じ ばしょ | 画面そと 0・まえ 0・ちがう ばしょ 21〜22 いじょう |
| 27 にん とちゅう U ターン(s 2000 で 引き返し forest へ もどる) | 回りこみに かかる じかん | まえに 出るのは さいだい 2 にん、2.4〜2.6 秒で 全員 うしろへ。それいご 画面そと 0・まえ 0 |
| 27 にん forest → home | 同上 | 画面そと 0・まえ 0 |
| 着いた あとの region(forest / home) | 同上 | 画面そと 0・まえ 0・27 にん 全員 ちがう ばしょ |
| じぶんへの 当たり | なし(なかまは じぶんを ふさがない) | 既存 24 場面の あるく じかん・段・UI は まえと 同じ |

- U ターンの 回りこみは、はじめ 23 にん が 3 秒 いじょう じぶんの まえに のこって いた。原因は corridor の 2 かいめ からの はやあし(1.4 倍 = 364/秒)に なかまの 上限(476/秒)が ほとんど 追いつかない こと。上限に `speedMultiplier` を かけて、まえに 出るのは 2 にん・2.5 秒で もどる ように なった
- 引き返し・曲がりでは カメラが なめらかに まわり、ならびも それに あわせて まわる(1 frame の うごきは 40 未満。テスト 7)

### 16.8 判定

- 横一列は なくなった(region / corridor / 着いた とき)。人数ごとの かたち・せまい 段の ほそい ならび・27 にん・U ターン・セーブ なし・決定的 は テストと 実画面で たしかめた
- 性能: ならびの 計算は 0.01 ms。27 にん の corridor は「全員 画面に いる」ぶん p95 +5〜10 ms、着く 暗転も すこし ながい。8 にん までは まえと 同じ
- **party formation は 採用**。ただし Phase 4E-2 ぜんたいの 判定は §15 と 同じ **「修正して採用」の まま**(forest に 着く 暗転が CPU 4 倍で 350 ms を こえる ことが ある・27 にんの corridor p95)。#337 は Draft の まま に する
